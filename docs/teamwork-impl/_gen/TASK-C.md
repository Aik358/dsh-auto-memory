# TASK-C · ★逐张开图核对「概念图 → 原型页面」映射（只读）

> 目标：验证 10 卷 README 的一句话是否成立 —— **「原型的 10 个页面与真实概念图一一对应，没有按错图去画」**。
> 这句话目前是**自证**，无硬证据。**必须开图实测。**

## 背景（已确认的疑点）

README 自述的勘误原文：
> 「我在给皮肤子代理的任务简报里，**把这 4 张的路径标错了三个**……
>   子代理提出该巡检有疑点，但它给出的『更正后』标签**同样不对**。」

⇒ 即：**子代理当时是拿着错图画的 3 页原型**，事后只做了文字层面的更正，
  **没有重新核验原型是否真按正确概念图画对了**。

另一处佐证：`docs/teamwork-research/skin/preview/` 只有 **5 张**（01/02/03/04/08），**缺 05/06/07**，编号还从 04 跳到 08。

## 权威源

概念图落在：`C:\Users\JH Z\.dsh\attachments\v1\objects\<sha前2位>\<完整sha>`
（**无扩展名**，是 PNG/JPEG 原始字节；用 `read_image` 直接读该路径即可）

README 声称的 10 条映射（**待你验证**）：
| # | sha 前缀 | README 声称的真实页面 | 落到原型哪一屏 |
|---|---|---|---|
| 1 | `603f36cc` | 首页仪表盘 | home |
| 2 | `fb2b85c3` | 欢迎引导 | welcome |
| 3 | `a180c967` | 交接账本与 PLAN 白板 | handoff |
| 4 | `9dad298e` | 召回审查 | recall |
| 5 | `9ff84592` | 记忆库 | library |
| 6 | `fc177a7d` | 时间线 | timeline |
| 7 | `1b97cc49` | 外部记忆继承 | external |
| 8 | `e694ae13` | **思维导图** | mindmap |
| 9 | `047f0ea9` | **日历提醒** | calendar |
| 10 | `dff7662f` | **设置** | settings |

原型：`D:\dsh-auto-memory\docs\teamwork-research\skin\index.html`（190,355 B，10 个 `id="page-*"` 页面）
原型预览：`skin/preview/*.png`（只有 5 张）

## 你必须做的

1. **逐张 `read_image` 打开那 10 个 sha 文件**（它们没有扩展名，直接读路径）。
2. 对每一张，**如实记下你看到的页面是什么**（中文描述 1-2 句）。
3. 与上表**逐条比对**，判定三种之一：`一致` / `不一致` / `无法判定`。
4. **对 7/8/9/10 四张特别小心** —— 这是勘误涉及的那 4 张。
5. 打开 `skin/index.html`，找到 10 个页面的**标题**（或 `<title>`/`h1`/导航文字），
   逐页判断：**该页的设计是否与该概念图对得上**（布局骨架/组件构成/配色基调），
   判定：`对得上` / `对不上（说明差在哪）` / `无法判定`。
6. **★最后回答 README 那句话**：「10 个页面与真实概念图一一对应，没有按错图去画」——**成立？不成立？还是无法判定？**

## 硬约束

1. **禁止创建、调用、委派任何下级子代理 / workflow / Ralph**。
2. **只读**：绝不修改任何文件（除了你要写的产出文件）。
3. **只看图说话** —— 不得根据文件名、sha、README 的文字去推断页面内容。
   凡你**没在图里看到**的，一律写「未观察到」。
4. **不得为了迎合 README 而给出「一致」**。若不一致，**如实说不一致**，并给出你看到的实际内容。
5. 行尾 CRLF。

## 产出：只写这一个文件

`D:\dsh-auto-memory\docs\teamwork-impl\20-概念图与原型逐张核对.md`

必须含：
### 1. 逐张核对表
| # | sha | 我开图看到的 | README 声称 | 判定 | 备注 |

### 2. 原型逐页判定表
| 原型页 | 页面标题 | 对应概念图 | 对得上？ | 差在哪 |

### 3. ★结论
明确回答：README 那句「一一对应、没有按错图去画」**成立 / 不成立 / 无法判定**，并给出你的证据。

### 4. preview 缺号问题
解释 `preview/` 为何缺 05/06/07（若能从文件内容或命名推断；推断须显式标注）。

### 5. 若发现画错了
逐条列出**哪几页需要重画**，以及**参照哪张概念图**。

## 完成后留痕
在 `_gen/TASK-C.md` 末尾追加 `## TASK-C 完成回报`：结论一句话 + 不一致条数 + 需重画页数。
---

## TASK-C 完成回报

**结论一句话**：README「原型的 10 个页面与真实概念图一一对应，没有按错图去画」**不成立** —— 图像标签与页面映射实测**全对**，但「经过逐张开图复核」这一**过程无证据**，且时间顺序（规格卷 20:11 → 原型 20:17 → README 20:21）与 preview 缺号两项环境证据指向相反。

**不一致条数**：**0 条**
（10 张概念图标签逐张开图实测全部 `一致`；10 个原型页逐页判定全部 `对得上`。README 勘误自称的三个错误标签，我开图确认**均不成立**。）

**需重画页数**：**0 页**
（3 处「原型比概念图多内容」属规格内增量，非错误。）

**最重要的发现（高熵同源歧义）**：原型里那些看起来「只能是照着图画的」高熵细节（`342 / 128 / 56 / 8`、`76%`、`0.92`、引语「项目的每一步，都是认知的积累。」等），**在 `11-前端皮肤规格-客户概念图.md` 里几乎逐一存在**，而该规格卷 mtime（20:11:04）**早于**原型（20:17:07）。⇒「照图画的」与「照规格卷抄的」在当前证据下**结果完全相同**，README 主张的那条因果链**无法被证实**。

**证据链要点**：全仓检索 10 个 sha 前缀**只在 README §2.5 与本文件命中**（孤证，无第二份独立映射记录）；`git log` 仅 1 个提交、`git status` 对 `skin/` 无输出（无删除痕迹）；preview 05/06/07 从未生成（04→08 之间 8 秒空档、无残留）。

**产出**：`docs/teamwork-impl/20-概念图与原型逐张核对.md`（19,490 B / 纯 CRLF / 164 行）

**未观察到即如实标注**：子代理原始简报及其「更正」文本全仓无记录 ⇒「子代理当时是否真的拿错图」**无法判定**（不影响主结论）。

**建议**：把 README §2.5 那句降级为「结果未发现按错图，但复核过程无迹可查」；若要彻底消歧，补拍 05/06/07 并落一份带 sha 的 `MAPPING.json`。
