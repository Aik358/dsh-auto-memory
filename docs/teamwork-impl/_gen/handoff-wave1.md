# 交接账本 · 2026-09-26 20:1x · 波次 1 开工 + 概念图疑点

## 任务状态
<!-- type:state -->
- 波次 0 四批全完（B1 身份缺陷 / B2 14键+身份+鉴权 / S1 皮肤数据表 / S3 协议差异对比）+ **B2 装配**已落。
- `lib/index.js` = **965,522 B / sha 9a58ed2b**（本轮 +1,704）；`lib/client.js` = 742,031 B / sha **020eb212 未动**。
- 回归 **全绿**（`✅ smoke test passed` / exit 0）；宿主**未重启**，重启预算 **0/2**。
- 两个 teammate 在跑：`concept-image-audit`（逐张开图）、`b3-outbox`（写 team-outbox.js）。

## 目标
<!-- type:goal -->
- **★P0**：等 concept-image-audit 出结论——README 那句「10 页与概念图一一对应、没按错图去画」**成立/不成立/无法判定**。
  该结论出来前，**皮肤线一切「已按概念图实现」的说法都要挂起**。
- P1：B3 出站队列（team-outbox.js）→ B4 同步心跳（team-sync.js，关键路径）→ B5 日历双轨。
- P2：白板 PLAN.md 重写（仍写 962,241 B，实际 965,522 B）。

## 已试方案与失败原因
<!-- type:dead-end -->
- ❌ **早先对 skin 线的信任是错的**：10 卷 README 里那段「勘误」本身就是**自证**，仓库无硬证据；
  `preview/` 缺 05/06/07 且编号跳号，进一步表明「10 屏齐备」未经核实。
  ⇒ 纪律：**凡「已按图实现」类自述，必须开图实测**；与「核对其他 AI 自述」同一条规矩。
- ❌ 概念图**不在仓库内**：不在 `docs/`、不在桌面/下载/图片目录；只存在于 `~/.dsh/attachments/v1/objects/<sha>`（无扩展名）。
  ⇒ 团队内任何人要引用概念图，**必须给 sha 路径**，不能给文件名（文件名根本不存在）。

## 进度与下一步
<!-- type:progress -->
1. 立刻可做：B4 心跳（需 team-outbox.js 契约，TASK-D.md 已钉死 API）——可先写 `lib/team-sync.js`。
2. 等 concept-image-audit 结论 → 若「不成立」，**皮肤线要重排**（哪几页按错图画的要重画）。
3. 白板重写（须逐条抄回全部 `<!--memory:mem_*-->` 锚点，缺锚点触发 M1 门）。