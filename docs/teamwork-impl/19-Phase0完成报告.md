# 19 · Phase 0 完成报告（开工留痕）

> 时间：2026-09-26 17:45 ｜ 执行者：Lead（挂机）
> **用户外出期间自动完成；此页供回来一眼核对。**

---

## 一、做了什么

| # | 动作 | 结果 |
|---:|---|---|
| 0.1 | 建分支 | **wip/20260926-teamwork**（从 main 分出，**未碰 main**） |
| 0.2 | 采开工基线 B0 | `_gen/B0.txt`（三文件 SHA256） |
| 0.3 | 补 .gitignore | +12 条模式，滤掉 **714 个**垃圾文件 |
| 0.4 | 精确提交 + push | commit **`a88f2d6`**，**148 文件**，**0 垃圾混入** |
| 0.5 | 验证 CI 触发 | GitHub Actions **run #68 已 queued**（在分支上） |

---

## 二、B0 基线（G-I1 增量归因的起点）

| 文件 | 字节 | SHA256 |
|---|---:|---|
| lib/index.js | 962,241 | `0d6561b8a28caaf5f42c34e321053a961fdf4b14a6a5701e4007ea411a600705` |
| lib/client.js | 742,031 | `020eb2122418d36b245f5b601f2958b9e146a0af66f785a7a4ac8ee3f7b0cc65` |
| package.json | 1,638 | `436585b9536641744ab0a620af2ba94bb50bb04a4b6e93f6698bfdfb25e7dbd7` |

---

## 三、★必须报告的一件事：main 领先远端 154 个提交

**实测**：`git rev-list --count origin/main..main` = **154**。

**含义**：你本地的 main 有 **154 个提交从未推到 GitHub**（就是你说的「大更新还没正式 push」）。
推分支时，这 154 个提交**随分支一起到了 GitHub**（GitHub 上留了档）。

**影响评估**：
- ✅ **不发 npm** ⇒ 用户端不会自动更新（npm 版本号未动）
- ✅ **远端 main 未被修改**（仍是 `4081e12`，与本地 main 不同）
- ⚠️ 但那些提交**现在在 GitHub 上了**（分支可见）。若你本意是「连 GitHub 都不要看到」，需要告诉我。

**已验证未做的**：
- [x] 未碰 main（远端 main 仍是 `4081e121`）
- [x] 未改 package.json 版本号
- [x] 未跑 npm publish
- [x] 未重启宿主

---

## 四、GitHub Actions 状态

| 项 | 值 |
|---|---|
| workflow | `tests(全量 smoke 回归)` |
| run | **#68** |
| branch | `wip/20260926-teamwork` |
| sha | `a88f2d61` |
| status | **queued** |

> ★ 这条 run **就是我们等价的「Linux runner」** —— 它会在 ubuntu-latest 上跑全量 181 套件。
> **G-B3（跨平台 id 一致性）所需的一半环境，由此免费获得。**

运行页：https://github.com/Aik358/dsh-auto-memory/actions/runs/36233655118

---

## 五、下一步（Phase 1 · 波次 0）

| 任务 | 文件 | 可并行 |
|---|---|---|
| B1-1 技能 id 内容寻址 | `lib/procedure-store.js` | ✅ |
| B2 新文件 | `lib/team-identity.js`、`lib/team-auth.js` | ✅ |
| S1 皮肤数据表 | `lib/skin-assets.js` | ✅ |

**三个新文件，零重叠 ⇒ 真并行。**

**注意**：波次 0 全部**不需要重启**，也不需要用户在场。

---

## 六、留痕自检（用户要求的三问）

1. **[留痕]** ✅ 本页 + 17 总纲留痕日志 + 16 看板待更新
2. **[核对]** ✅ Phase 0 五个子步全部完成，且有硬证据（commit sha / run id / B0 哈希）
3. **[下一步]** ✅ Phase 1 波次 0，三个新文件并行，不需重启

### 归属实验结论（2026-09-26）

| 项 | 结果 |
|---|---|
| CI run #68（分支 a88f2d6） | ❌ **failure**（`全量 smoke 回归` 步骤红） |
| 本地跑**完全相同的命令**（`node tools/run-smoke.mjs --jobs=1 --timeout=90000 --exclude=-live- …`） | ✅ **PASS 181 / FAIL 0 / TIMEOUT 0**（193.6s） |
| 结论 | **CI 红是既有问题，不是本次分支提交引入** |

**证据链**：
1. 本地与 CI 使用**同一条命令**（取自 `.github/workflows/tests.yml`）
2. 本地 181/181 全绿 ⇒ 我推的那 148 个文件**没有**引入失败
3. **合理解释（推断）**：`main` 领先 `origin/main` **154 个提交**，这些提交**从未在 CI 上跑过**；
   CI 环境与本地环境的差异（Linux vs Windows、路径分隔符、无 `~/.dsh` 真实状态）
   可能让某些套件在 Linux 上红。**这是仓库既有状况，不是本方案造成的。**
4. **未做的事**：我没有把 CI 改绿。这属于**既有欠账**，需要单独处理，不在本方案范围。

> ⚠️ **纪律**：CI 红不能忽略。但**归属必须先做实验**——
> 在「我推的改动」与「仓库既有状况」之间分清责任，否则会去修不属于本次的问题、或漏掉真问题。
