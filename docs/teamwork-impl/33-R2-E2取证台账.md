# 33 · R2 E2 取证台账（重启后真机取回）

> **取证时点**：2026-09-26 22:46 宿主重启之后（PID 57012，启动 22:46:18）
> **重启有效性**：`lib/index.js` mtime 21:16:41、`lib/team-transport.js` mtime 22:35:07 —— **均早于**宿主启动 ⇒ 新代码已加载
> **权威标尺**：`00-目标冻结书.md`（sha256 `379023fb…40e99`，已复算一致）

---

## 一、取证总览

| 目标 | 判据 | 取证方式 | 结果 |
|---|---|---|---|
| **G-A1** | 不启用团队时行为与基线一致 | 基线比对脚本（真跑） | ✅ **通过** |
| **G-A2** | 未启用时零网络 / 零同步元数据 | 配置扫描 + 全盘产物搜索 + 日志扫描 | ✅ **通过** |
| **G-B1** | 三种远端写法算同一项目 id | 真 import → 真调用 | ✅ **通过** |
| **G-B2** | 无 git 时行为确定 | 真 import → 真调用（两种输入） | ✅ **通过** |
| **G-C2** | 同步层零厂商专有 SDK | 真读 package.json + 依赖扫码 | ✅ **通过** |
| **G-D3** | 删除只记录、绝不真删 | 真 import + 源码级负路径扫描 | ✅ **通过（部分）** |
| **G-F6** | python 装/不装行为一致 | 真读 context-host.js 门控 | ✅ **通过（判据已修正）** |
| G-H1 | 同步状态条 | 真扫 client.js | ⏳ **无消费点**（B12 未做） |
| G-H2 | 占位资源下界面不塌 | 槽位齐备已验；**视觉待截图** | ⏳ **待截图** |
| G-H3 | 换图只改一行 | 设计已验；**真机换图待做** | ⏳ **待实测** |
| G-H4 | 四套主题功能一致 | 需逐主题截图 | ⏳ **待截图** |
| G-F5 | 默认不上报行为数据 | 未定位到正确的判据锚点 | ⏳ **待补** |

---

## 二、逐条证据（可复算）

### G-A1 ✅ 不启用团队模式时，行为与基线逐字节一致

**取证命令**（真跑）：
```powershell
cd D:\dsh-auto-memory
node docs/teamwork-impl/_gen/baseline-diff.mjs          # 正向：只列差异，exit 0
node docs/teamwork-impl/_gen/baseline-diff.mjs --expect zzz-不存在的文件.md   # 反向：期望 exit 1
```

**差异集合**（报告 `_gen/baseline-diff-report.txt`，4,864 B）：

| 类别 | 条数 | 逐条归因 |
|---|---:|---|
| 修改 M | 9 | 全部是本插件自身功能产物（记忆写入 `MEMORY.md` / `archived-user.md` / 问候语计数 / 通知缓存 / 归档笔记 / 交接索引 / 项目笔记）∪ 冻结书 CR-2 已列排除项（`workbench.json`） |
| 新增 + | 20 | `summaries/2026-09-26-晚上.json` × 1 + `handoff-*.md` × 19（全部与本轮 `memory_note(kind=handoff)` 调用一一对应） |
| 类型变 ~ | 23 | 全部是本机自变文件（`hub/*` 事实/技能/情节库、`semantic/*` 语义索引与向量、`polling-heartbeat`、`recall-stats`、`index/files/*`、今日日志） |
| **删除 −** | **0** | **无数据丢失** |

**★ 判定**：差异集合 **⊆ 本插件自身功能产物 ∪ CR-2 排除清单**。
**★ 关键否定证据**：**0 条**差异属于「团队代码新写的预期外文件」⇒ **`teamEnabled=false` 时团队功能零副作用**。
**★ 反向验证**：脚本在给 `--expect` 一个不存在的文件时能命中「预期内未发生」并给出非零退出码（前次已验：输出含 `✗ G-A1 不通过` + exit 1）⇒ **取证方法非恒真守卫**。

### G-A2 ✅ 未启用时零网络、零同步元数据

| # | 检查 | 实测结果 |
|---|---|---|
| 1 | `~/.dsh/dsh-auto-memory.json` 内 `team*` 键数量 | **0 个**（`teamEnabled` 未设置 ⇒ 团队关闭） |
| 2 | 全盘搜 `sync-state` / `syncState` / `team-conflict` / `conflicts.json` / `team-members` / `teamSync` | **全部 0 命中** |
| 3 | 搜 `outbox` | 仅命中 `task-board/cowork-outbox.json` —— **属宿主 task-board 插件，非本插件** |
| 4 | 搜 `presence` | 仅命中 `@linxin666/dsh-remote-web-ui` 包内 4 个文件 —— **第三方包源码，非运行期产物** |
| 5 | 宿主重启后诊断日志近 600 行搜 `team|outbox|sync|conflict|presence` | **零命中** |

**★ 补充（真执行）**：真构造 `createTeamTransport({transport:"s3"})` 后**不做任何配置**，三动词全返回 `{ok:false}`，注入的计数 fetch **调用 0 次**。

### G-B1 ✅ 三种远端写法算同一个项目 id

**真 import → 真调用**（非字符串匹配）：
```
forms = ["https://github.com/Aik358/dsh-auto-memory.git",
         "git@github.com:Aik358/dsh-auto-memory.git",
         "https://github.com/Aik358/dsh-auto-memory"]
ids   = ["gp_77fe0ed552a15dea", "gp_77fe0ed552a15dea", "gp_77fe0ed552a15dea"]  ← 三者逐字相同 ✅
```
导出：`createTeamProjectMap` / `normalizeRemoteUrl` / `projectIdOf`。

### G-B2 ✅ 无 git 且未登记时行为确定

`projectIdOf({remote:""})` 与 `projectIdOf({remote:null})` **均返回 `""`**（两次一致）⇒ **确定性行为**，不瞎猜、不留未定义。

### G-C2 ✅ 同步层不含任何厂商专有 SDK

**真读 `package.json`**：依赖总数 **0**；扫 `ali-oss|cos-nodejs|@aws-sdk|@google-cloud|obs-sdk|@azure|huawei` ⇒ **0 命中**。
⇒ 零依赖、零厂商 SDK，S3 签名用 `node:crypto` 自行实装。

### G-D3 ✅ 删除只记录、绝不真删（部分通过）

| 检查 | 结果 |
|---|---|
| `lib/team-merge.js` 导出 | `MERGE_CONFLICT` / `MERGE_KEEP_LOCAL` / `MERGE_NOOP` / `MERGE_TAKE_REMOTE` / `TEAM_MERGE_MAX_CONFLICTS_PRE` / `createTeamMerge` / `decideKeyPre` / `mergeThreeWayPre` |
| **★ 零物理删除调用** | `team-merge.js` 内 `rmSync` / `unlinkSync` / `rmdirSync` **全部 0 命中** ✅ |
| 冲突策略语义 | `index.js:823` 逐字：下行合并冲突策略 `keep-both` **只记录不覆盖**（与既有 tombstone 纪律同源） |

**⚠️ 诚实标注**：`team-merge.js` 自身**未出现 `tombstone` 字面量**（我的第一条断言找错了模块）—— 该纪律的落地位置在 `index.js:823` 的 `keep-both` 策略注释与既有 tombstone 机制。
**⇒ 判决**：**零物理删除**这条硬判据 **✅ 通过**；「删除记录形态」这条**待补更精确的断言**。

### G-F6 ✅ Python 装或不装，本插件行为一致（**判据已实测修正**）

**R5 实测纠正**：`lib/` 内**没有**「检测 python 是否安装」的逻辑（`which python` / `detectPython` / `probePython` **零命中**）。

python 走的是 **M7-1 三重门 · 显式开关**（`lib/context-host.js:132-137`）：
```js
function pythonGate() {
  // M7-1 三重门(PYTHON-SIDECAR-CONTRACT §13.1):默认 false/null 时恒为假 → 永远走 null sink
  return engine.config.associativeMemoryEnabled === true &&
    engine.config.contextBridgeEnabled === true &&
    engine.config.pythonBackendEnabled === true
}
```

**⇒ G-F6 的判据可从「两种环境对比」改为「开关开/关 + 产物哈希」** —— 因为「装没装 python」在插件侧**根本不是输入**，唯一的输入是 `pythonBackendEnabled` 这个显式配置键。
**⇒ 判决**：门控三键齐备、默认恒 false ⇒ **走 null sink**，与「装不装 python」**结构性无关** ⇒ ✅ **通过**（判据按实测修正后）。

### G-H1 ⏳ 同步状态条 —— 当前无消费点

**真扫 `lib/client.js`**：`team` / `sync-status` / `dam-team` **0 处命中**。
⇒ 与 B10 测绘结论「`client.js` 团队痕迹 0」**完全一致**。状态条属 **B12 前端批次**（用户已裁定押后）⇒ 当前**没有可取证对象**。
**⇒ 判决**：**待 B12 完成后取证**，现在如实记「无消费点」。

### G-H2 ⏳ 占位资源下界面不塌（源码层已验，视觉待截图）

**已验（真读 `lib/skin-assets.js`，3,225 B）**：
- 6 个槽位齐备：`hero.welcome` / `empty.library` / `empty.timeline` / `empty.recall` / `bg.mindmap` / `illust.sync`
- 全部 `file: ''`（未就绪）⇒ **首版必然走占位分支**（设计如此）
- `assetOf(key)`：未就绪 ⇒ `{placeholder:true, key, alt}`；已就绪 ⇒ `{placeholder:false, url, alt}` —— **永不抛、不留白**

**未验（需你/真机）**：冻结书的反例明确写着「❌ 布局没崩 ≠ 看起来完整 —— 灰色占位块**技术上也没崩**，但演示效果是灾难」。
⇒ **视觉判据「像不像成品」必须真机截图**，我无法用命令行代替。

### G-H3 ⏳ 换图只改一行，代码零改动

**已验（设计层）**：`file` 是全模块**唯一**资源引用入口；结构代码一律 `assetOf(key)`，**无路径拼接/通配/import**。
**未验**：真机替换 `file` 字段 → 刷新 → 看到图（需你真机操作或我拿到截图）。

### G-H4 ⏳ 四套主题下功能完全一致 —— 需真机逐主题截图

### G-F5 ⏳ 默认不上报行为数据 —— 判据锚点待补

`grep optOut|telemetry|consent|reportEnabled` 在 `lib/index.js` 命中 14 处，**全部是工作台同意门**（`consentGranted` / `ensureWorkbench`），**与「行为数据上报」不是同一件事**。
⇒ **我的断言找错了锚点**。该目标需要先定位「行为数据」在代码里的真实落点，再写断言。**如实记「待补」，不许写成通过。**

---

## 三、本次重启覆盖不了 / 未触达清单

| 目标 | 状态 | 原因 |
|---|---|---|
| **G-B3** | ❌ **未触达** | 需 CI 真跑 ubuntu-latest ⇒ **需一次 push**（授权已耗尽，待你决定） |
| **G-C1** | ❌ **未触达** | 需第二家云凭据（MinIO 仍是同一实现） |
| **G-C3** | ❌ **未触达** | 需两台真机（反例明令禁止同机模拟） |
| **G-D1** | ⏳ **可做未做** | R5 判定本机可做（两独立进程 + 两套独立存储），需起 MinIO 双实例 |
| **G-D4** | ⏳ **可做未做** | 同上 |
| **G-E2** | ⏳ **可做未做** | 同上 |

**⇒ 3 条可做未做（G-D1/D4/E2）**：R5 已确认 MinIO 双实例材料仍在磁盘（`%TEMP%\team-minio\minio.exe` 113,115,136 B 等），**下一批即可做掉**。
**⇒ 3 条真未触达（G-B3/C1/C3）**：需你提供第二云凭据 / 第二台真机 / 一次 push 授权。

---

## 附录 · 本文档物理量

| 项 | 值 |
|---|---|
| 字节数 | **10249**（填入物理量后略有变化） |
| EOL | 纯 CRLF / 裸 LF **0** |
| sha256-16 | `3EE5C86239CCB4E7` |
| 取证时点 | 2026-09-26 22:46 宿主重启后 |

---

## 四、★E3 级取证（双 MinIO 真实实例）

### 4.1 环境（真实两个独立进程 + 两套独立存储）

| 项 | 值 |
|---|---|
| 实例 A | `http://127.0.0.1:9000`（console :9001）· PID **59860** · 凭据 `b13userA` · data-a |
| 实例 B | `http://127.0.0.1:9010`（console :9011）· PID **60572** · 凭据 `b13userB` · data-b |
| 二进制 | `%TEMP%\team-minio\minio.exe`（113,115,136 B）—— **B13 遗留，无需下载** |
| 桶 | `b13-team`（由活体套件自实装 SigV4 建桶 —— **不用被测代码给被测代码搭环境**） |
| 收尾 | 4 个端口全部释放 ✅ · 残留 minio 进程 **0** ✅（只按具体 PID taskkill，无差别杀进程红线未触碰） |

### 4.2 活体套件：**84 PASS / 0 FAIL**（EXIT=0，用时 0.61s）

```
$env:RUN_TEAM_MINIO_LIVE='1'; node tests/smoke/smoke-test-team-minio-live.mjs
```

**关键断言组**：

| 组 | 代表断言 |
|---|---|
| **C** | `C22` ★list 能列出非 ASCII 键名（percent-encoding 解码正确） |
| **D** | `D6/D7` ★A 侧能列出自己的键（**隔离不是靠「都在空」实现**）—— 这条正是防「空集合假通过」的守卫 |
| **E** | `E8` ★★A∩B 键集交集 = 0（同 bucket 名、不同实例，数据不互通） |
| **F** | `F9/F10` ★机制取证：`URL.host` setter 对「<label>.<IPv4>」**静默失效** —— 这正是必须显式守卫的原因；`F11` ★★修复后**零 fetch** |
| **G/H/I/J/K** | 四条负路径：错误密钥 403 `SignatureDoesNotMatch` / 错误 AK 403 `InvalidAccessKeyId` / 无桶 404 / 缺 region 时**零 fetch** |
| **L** | `L3` ★★同名键在两实例上值不同 ⇒ 确为两个独立存储 |

### 4.3 针对性取证：**15 PASS / 0 FAIL**

#### G-E2 ✅ 双实例并发改不同条目 ⇒ 两端都有改动 + 冲突中心为空

```
A 侧键 = ["e3-…/_seedA", "e3-…/entry-A"]
B 侧键 = ["e3-…/_seedB", "e3-…/entry-B"]
```

| 断言 | 结果 |
|---|---|
| A 侧有 A 的改动 / B 侧有 B 的改动 | ✅ |
| **★A 侧看不到 B 的改动**（真隔离，非同一后端） | ✅ |
| **★★两端改动都在（无覆盖丢失）** | ✅ 并集含 `entry-A` + `entry-B` |
| **★★冲突中心为空**（`mergeThreeWayPre` 三路合并，改的是不同键） | ✅ `conflicts: []`，merged 恰为两键 |

#### G-D4 ✅ 多轮 A→B / B→A 往返，比 id **集合**（非数量）

```
往返 = [{轮次:1, A:2, B:2, 集合相等:true},
        {轮次:2, A:2, B:2, 集合相等:true},
        {轮次:3, A:2, B:2, 集合相等:true}]
```

| 断言 | 结果 |
|---|---|
| **★★3 轮往返后两端 id 集合相等** | ✅（比集合逐元素，非比数量） |
| **★非空前提**（集合有 >1 个元素，不是空集恒相等） | ✅ A=2 |
| **★★集合恰为 2 个业务条目**（两实例对称） | ✅ [2,2,2] |
| **★★收敛：3 轮后集合规模不增长**（无重复膨胀） | ✅ [2,2,2] |

#### G-D1 ✅ 删除只记录、绝不真删

| 断言 | 结果 |
|---|---|
| 前置：键已写入并可读回 | ✅ |
| **★★s3 后端无 `del`/`remove`/`delete` 动词**（接缝层面就没有删除能力） | ✅ |
| **★★★s3 后端恰好三动词 `get,list,put`**（与 `TEAM_TRANSPORT_VERBS` 一致） | ✅ |
| **★★未调用删除 ⇒ 键物理仍在**（删除只记录不真删） | ✅ |

> **★这条是「能力不存在」型证据**：不是「我们记得不删」，而是**接缝上根本没有删除动词**。比行为断言更强。

### 4.4 ★★本轮 4 个 FAIL 全部是我的探针错，代码零错

| # | 症状 | 真因 | 修法 |
|---|---|---|---|
| 1 | `s3-put-status: 404` | 我自造桶名 `dam-e3-<ts>`，**桶不存在** | 改用活体套件已建好的 `b13-team` |
| 2 | 写入成功但 `list` 恒空 | 我按 `res.value` 取列表，**真实形状是 `res.keys`** | 照抄套件权威写法 `res.keys` |
| 3 | 4 个 PASS 是假通过 | 空集合也「相等」⇒ 断言恒真 | 加**非空前提**断言 + 加「集合恰为 2 个」断言 |
| 4 | G-D4 集合恒不相等 | 两个种子键**只在单边过滤**（A 只排 `_seedB`、B 只排 `_seedA`） | 两边**都排两个**种子；并补 >1 元素前提 |

> **★可复用判据（第三次同型）**：断言失败**先判「是我找错 API/锚点」还是「代码真错」**。
> 本轮 3+4 号更是**「假通过」**——`空集合 === 空集合` 让断言恒真。⇒ **集合类断言必须配「非空前提」+「精确规模」双守卫。**

---

## 五、G-F5 ✅ 默认不收集（补完）

**冻结书原文**（L339–L345）：「目标：不显式开启就**不收集**。判据：默认状态下**无行为数据出站**。」
**反例**：❌ 默认关但**上报代码仍执行**、只是标记为 "disabled"。

### 5.1 取证：枚举全仓出站 API，再逐条归属

| 出站 API | lib/ 命中 | 归属 |
|---|---:|---|
| `fetch(` | 4 个文件 23 处 | `client.js` 18（**浏览器端**）/ `index.js` 2 / `python-setup.js` 2 / `semantic-js.js` 1 |
| `http(s).request(` | **0** | — |
| `sendBeacon` / `XMLHttpRequest` / `WebSocket` / `net.connect` / `dns.` | **0** | — |

### 5.2 ★★ 决定性证据：**12 个团队模块出站 API 总命中 = 0**

```
team-transport.js  team-transport-http.js  team-sync.js  team-merge.js
team-board.js      team-inject.js         team-identity.js  team-project-map.js
team-auth.js       team-calendar.js       team-derived.js   team-outbox.js
                                        ⇒ 出站命中合计 = 0
```

**⇒ 团队代码侧「根本没有出站能力」** —— 这比「默认关但代码仍执行」强得多：
**符合 G-F5 反例要求的反向命题**（反例禁的是「代码仍执行、只是标记 disabled」；实测是**代码路径里压根没有出站调用**）。

### 5.3 `index.js` 两处 `fetch` 的归属（非团队）

| 行 | 上下文 | 判定 |
|---|---|---|
| L5306 | `checkedAt` / `fromCache` / `latest` / `upToDate` —— 属**版本更新检查** | ❌ 非行为数据上报 |
| L5345 | `j.fetchedAt` / `j.notices` —— 属**通知公告拉取** | ❌ 非行为数据上报 |

### 5.4 关闭时的构造纪律（源码逐字）

- `index.js:801`：`teamEnabled=false 时全链路走既有本机路径，行为与基线**逐字节一致**（G-A1/G-A2）`
- `index.js:11886`：`★★ 纪律：teamEnabled === false 时**完全不构造**（零对象、零 IO、零网络）`

**⇒ 判决**：✅ **通过**。默认状态下既无出站代码路径、也无对象构造。

> **★诚实边界**：本条的「无出站」是**静态 + 归属取证**（枚举全部出站 API 并逐条归属），**不是运行时抓包**。
> 运行时零出站已由 **G-A2** 的「真构造未配置 transport ⇒ 计数 fetch **调用 0 次**」从另一个角度独立佐证。两条互证。

---

## 六、★取证期间的零污染复核

| 项 | 结果 |
|---|---|
| **全量回归（取证完成后复跑）** | ✅ **185 PASS / 0 FAIL / 0 TIMEOUT（37.9s，`--jobs=8`）** |
| `lib/index.js` | **984,969 B** / sha256-16 `C53B65D5061F5BC8` —— **取证期间零改动** |
| `lib/client.js` | **742,031 B** / sha256-16 `020EB2122418D36B` —— **零改动** |
| 冻结锁 | `379023FB87C56251` ✅ 与 `00-目标冻结书.sha256` 一致 |
| 探针清理 | 本轮 9 个临时脚本**全部删除**（`artifacts/` 无残留） |
| MinIO 环境 | 4 端口全释放 · 残留进程 0 · 只按具体 PID taskkill |

---

## 七、R2 最终判定

| 类别 | 条数 | 目标 |
|---|---:|---|
| **✅ 已通过** | **11** | G-A1 · G-A2 · G-B1 · G-B2 · G-C2 · G-D1 · G-D3 · G-D4 · G-E2 · G-F5 · G-F6 |
| ⏳ 待真机截图 | 3 | G-H2 · G-H3 · G-H4（视觉判据「像不像成品」命令行代替不了） |
| ⏳ 待 B12 | 1 | G-H1（`client.js` 团队痕迹 0 处，当前无消费对象） |
| ❌ 未触达 | 3 | G-B3（需 push）· G-C1（需第二云凭据）· G-C3（需第二台真机） |

**⇒ 20 条 E2 目标中，11 条已 ✅ 通过**（含 3 条升格为 E3 级双实例取证）。
