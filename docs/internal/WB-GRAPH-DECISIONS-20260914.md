# WB-GRAPH 拍板点 · 一页纸（2026-09-14 整理）

> **用途**：把散在 `WB-GRAPH-INTEGRATION-PLAN.md`（386 行）里的决策点集中到一页，你**直接在"结论"列写 yes/no 或回我一句**即可。
> **来源**：方案 §8（8 点）+ §10（看板化 7 点）；**你的方向已定**：把看板/流程图 **combine 进自己的白板**，让用户实时看到进展与整个项目的流程（= §10 的看板化）。
> 状态约定：✅ 已定 / 🎯 待拍板 / ⏸ 冻结后再说。**P0-P1 不等这些拍板点，可立即做。**

---

## A. 方案 §8 的 8 个拍板点

| # | 问题 | 选项 | 方案建议 | 影响 / 代价 | 结论 |
| --- | --- | --- | --- | --- | --- |
| A1 | **P2/P3 是否立项** | 立项 / 等冻结解除 / 不做 | P0-P1 立即做；**P2-P3 等后端冻结解除后按方案执行** | P2/P3 都改 `lib/index.js` 本体，冻结期内禁改引擎 | 🎯 |
| A2 | **sidecar 位置** | `memoryRoot/<ws>/handoff/` vs 工作区 `.dsh-memory/handoff/` | **前者**（与现有 handoffDir 同目录；GUI 白名单只放行 `.json`，备份/迁移自动覆盖） | 选错则备份/迁移要另写规则 | 🎯 |
| A3 | **静态纪律是否随下一版一次性更新** | 更新 / 不更新 | **更新**（把判据纪律写进固定 section，而不只在工具描述里） | 代价：全量会话一次缓存重建 | 🎯 |
| A4 | **工具命名** | `memory_expand`/`memory_trace`（对齐现有 14 工具 `*_pre` 惯例）vs 任务书直译 `expand_tag`/`trace_back` | **前者** | 命名一旦发布就难改（对外契约） | 🎯 |
| A5 | **是否进 2.6.0** | P0-P1 进 2.6.0 / 全部等 2.7.x | **P0-P1 进 2.6.0；P2-P3 进 2.7.x**（工具数变化 + 存储格式新增，宜独立回归窗口） | 影响发版节奏与回归范围 | 🎯 |
| A6 | **P1 水位骨架硬判据失败策略** | 照写 + 警示行 / 跳过写入 | **照写 + 警示行**（优先保证接续材料存在） | 跳过写入更"干净"但接续会缺材料 | 🎯 |
| A7 | **PLAN 判据强度** | 维持自由节名 + 最弱硬判据 P-H1 / 固定 PLAN 小节集 | **维持自由节名**（P7 老化和"全貌图"用途依赖节名灵活性） | 固定小节集会削弱老化机制 | 🎯 |
| A8 | **P2-6 条目锚点是否做** | 做 / 不做 | **做**（它是"按 id 展开"的地基；+30 行） | 涉及锚点写法与 `sanitizeReservedSyntax` 豁免约定 | 🎯 |

---

## B. 看板化（§10）的 7 个待解点 —— 你已定方向，这些必须一起解决

| # | 问题 | 方案给的解法 | 我的补充建议 | 结论 |
| --- | --- | --- | --- | --- |
| B1 | **丢卡风险（最严重）**：白板是整篇重写，看板丢卡 = 任务蒸发 | sidecar 重写前后**卡片集合对比**（只能移动/完成，不能消失）→ **看板化使 P2 从"可选"变"刚需"** | 与 P0-④ 的 `supersede` 同源：**任何"消失"都要留痕** | 🎯（P2 顺位因此上调） |
| B2 | **旧卡撑爆截断**：`carryText` 第 0 层 `slice(0,3000)` 被已完成卡挤占 | 列顺序约定 + 完成卡归档；**P7 老化（按标题关键词）对看板列名失配，需升级为列感知** | 归档 = 移到 sidecar 的 `archived` 集合，白板只留活跃卡 | 🎯 |
| B3 | **卡片腐烂**：状态漂移误导接续 | 卡片带接续链 `contSeq` 时间轴 + 刷新仪式逐卡对状态 + 僵尸卡注入时打过期标记 | 复用现有 `cont-seq.json` | 🎯 |
| B4 | **人机冲突（核心新问题）**：你改卡 vs 模型整篇重写互相覆盖 | (a) 只读 (b) 卡 id + 接口合并（依赖 P2-6，工程最大）(c) **卡片分「模型维护区 / 用户备注区」** | **从 (c) 起步**，稳定后再评估 (b) | 🎯 |
| B5 | **双状态源**：账本四段与看板并存 | 看板 = 现在时（唯一事实）、账本 = 过去时（历史）；账本「任务状态」段改为指路 | 写进判据，由 P1 中间件校验 | 🎯 |
| B6 | **多任务混板** | 卡带接续链标记（Cue/contSeq），新窗口只展开自己链 | 与 §3 的 Cue 设计闭环 | 🎯 |
| B7 | **节奏** | P0 级零成本验证：**先出看板格式约定文档，真实工作区试两周**（模型守格式？接续取任务顺不顺？）再定 P1/P2 | 同意先试点 | 🎯 |

---

## C. 我的推荐组合（你只需说"按推荐来"或指出要改的点）

1. **A1**：P0-P1 立即；**A5**：P0-P1 进 2.6.0，P2-P3 进 2.7.x；**A2/A4/A6/A7/A8** 均按方案建议。
2. **B4 从 (c) 起步**（模型维护区 + 用户备注区），**B7 先试点两周**；**B1 使 P2 顺位上调**（与 P0-④ 的 supersede 同源，可同版实现）。
3. 界面渲染属 `lib/client.js`（**冻结外**），可与大排期合流；**引擎侧（P2/P3）等冻结解除**。

## D. 拍完我就做什么

| 拍板结果 | 立即动作 | 工程量 |
| --- | --- | --- |
| 按推荐 | ①写 `docs/HANDOFF-CRITERIA.md` 判据约定（P0，零代码）②实现 P1 判据校验中间件（≈190 行，咽喉 `writeHandoffLedger`/`writePlanSnapshot`）+ 回归（**工具数不变**） | 半天～1 天 |
| 允许 P2/P3 | 按方案 §5 改动清单执行（sidecar + 两工具；**工具数 14→16，三处测试硬锁同步**：`smoke-test.mjs:67` / mapi-paths 等） | 约 2 天 |
| 看板化 | 出「看板格式约定文档」→ 真实工作区试两周 → 再定交互方案 (a/b/c) | 试点期 |

---

## E. 2026-09-16 凌晨拍板结果（本轮已定，覆盖上方 🎯）

| # | 结论 | 备注 |
| --- | --- | --- |
| A1 | ✅ **P2/P3 立项**（用户选「立项」） | 3.0 主体收官，引擎冻结实质解除 |
| A2 | ✅ sidecar 放 `memoryRoot/<ws>/handoff/`（按建议） | |
| A3 | ✅ 静态纪律随下一版一次性更新（按建议） | |
| A4 | ✅ 工具命名 `memory_expand`/`memory_trace`（按建议） | |
| A5 | ✅ P0-P1 与 P2-P3 分期（按建议） | |
| A6 | ✅ 硬判据失败照写+警示行（按建议） | |
| A7 | ✅ PLAN 维持自由节名+最弱硬判据 P-H1（按建议） | |
| A8 | ✅ 条目锚点做（按建议） | |
| B4 | ✅ **裁定变更：搬 dsh-graph 接管看板层**（用户原话：「MIT协议说明可以直接将它的插件搬过来。按照原来的调研，直接让他接管记忆白板。让用户可以实时、可视化地看到项目从立项到现在的所有流程、弯路、版本更迭等事无巨细的信息；同时也可以让 AI 看到，作为接续的重要参考之一」） | 实测 v0.11.0 兼容性后用户拍板；**覆盖原「只借范式不搬代码」的自设边界**——MIT（`dsh-graph-host/LICENSE`）允许搬代码；MRAgent 无 LICENSE 的零复制约束不变。兼容性实测：Windows 原生 OK（v0.11 修复）、cordis 标准接线（name/inject/apply）、40 个 `graph_*` 工具与 14 个 `memory_*` 零重名、存储 `.dsh-graph/` 与 `memoryRoot/` 不重叠、运行时零 `@deepseek-ai/*` 硬 import |
| B7 | ✅ **跳过试点期直接实装**（用户原话：「跳过试点，直接实装，有bug，用户自然会报」） | WB-FORMAT-CONVENTION v1 已批准为实装依据，验收清单能失败断言兜底 |
| P1 范围 | ✅ **P1 全量**：判据校验中间件（两咽喉 ≈190 行）+ 锚点实装（A8）+ 丢卡门（B1）+ 静态纪律（A3），工具数不变 | 用户选「P1 全量（推荐）」 |
| 看板层 | ✅ dsh-graph 搬入接管：用户实时可视化全流程（立项/弯路/版本更迭）+ AI 可见作为接续参考 | 与 P1 判据门并行：P1 管写入质量，dsh-graph 管可视与流程展示 |

**立即动作序列（本窗口）**：①P1 判据校验中间件 + 锚点 + 丢卡门（`writeHandoffLedger`/`writePlanSnapshot` 两咽喉）→ ②dsh-graph v0.11.0 搬入（vendor 到本仓库 + profile 接线）→ ③全量回归 + 留痕 → 明晨交 GPT 验收。
