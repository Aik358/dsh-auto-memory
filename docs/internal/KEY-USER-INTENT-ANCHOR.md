# ★用户关键原话 · 地址锚（压缩后按此找回原文）

## 为什么有这个文件
会话压缩会丢失用户原话。此文件是**稳定地址**：压缩后不要再依赖摘要，按下面的路径与检索串去读**原始会话日志**。

## 原文位置（硬地址）
- 会话目录：`~/.dsh/sessions/--D-dsh-auto-memory--/session-80fc3e6d-24f5-4e40-ab11-c2c16a486622/`
- 文件：`session.jsonl.zstd` / `session.v4.jsonl.zstd`（**多帧 zstd，逐事件追加**）
- 检索串（唯一）：`子对话回收已经炸`
- 周边标记：同一行 JSON 内 `"rpcId":"140dde3a-c262-4aab-a9a2-7468b8963897"`、`"id":"9a1b81fe-1e1a-48b0-b2d7-7fdf462de779"`
- 时间：`"time"` 约为 1790184161xxx（epoch 毫秒）

## 读法（必须照做，否则会读成乱码）
1. 用 `lib/subagent-gc.js` 的 `scanZstdFrames(buf)` 逐帧切分；
2. 每帧单独 `zlib.zstdDecompressSync(frame)`；
3. 拼起来后再搜检索串。
> 反例：直接 `zstdDecompressSync(整个文件)` **只解第一帧**（实测 83MB 文件只读出 199 字符）⇒「什么都没找到」是假象。
> 可复用脚本：`artifacts/_r8-ctx.mjs`（已实现上述读法，改 SID/锚点即可）。

## 关键原话（逐字）
> 因为我的接续功能确实实践过这个新建对话的功能，它会有这样的接口可以用的，所以我才会有这种想法。目前我的初步想法是这个：0.1.7 版本子对话回收已经炸掉了，必须得重构 subagent 的逻辑了。
>
> 预计新版本将会自动新建一个工作区，然后在工作区下面新建对话，进行 consolidation、summary 等内容。这样还方便了以后做梦功能的使用，以及更多新花样的实践。这个工作台以月度更新或者以周度更新，你可以选择。工作台下的 subagent 用来做 greeting 和暂离问候，以每 10 个 greeting 为一个 loop，避免上下文的无序失控。你可以全量扫描一下我这个需要调子代理的功能，看一下这个实现的可能性，另外这个前端重构我还是不满意，但是已经有一个用户给我了一个范本，但是目前这个适配性更新更着急一点，那就先做这个吧

## 从上下文提取的**真实意图**（逐条）
1. **前提依据**：用户说「我的**接续功能**确实实践过这个新建对话的功能，它会有这样的接口可以用的」⇒ **接续功能就是参照实现**，用户要的是与接续**同一套接口/同一条路径**建会话，不是我另找的 `agents.create`。
2. 「自动新建一个工作区」+「在工作区下面新建对话」⇒ 两件事都要：**工作区可见** + **对话挂在它下面**。
3. consolidation / summary 等 = 在这个工作区下的对话里做。
4. 工作台**周度或月度**轮换（用户让我选）。
5. 工作台下的 subagent 做 greeting 与暂离问候，**每 10 个 greeting 一个 loop**，目的是**避免上下文无序失控**。
6. 「全量扫描需要调子代理的功能，看实现可能性」= 先做可行性核查（已完成）。
7. 前端重构：**明确推迟**（「适配性更新更着急」）。

## 当前实现状态（截至本文件写入）
- ✅ 工作台会话创建（`agents.create`）、三重校验、门控、居中弹窗、周/月轮换、greeting loop 计数、打开时复查
- ⚠️ **未验证**：工作区是否真的出现在侧栏 + 对话是否真的挂在它下面
- ❗ **待查重点**：参照「接续功能」的建会话路径；`workspaceRegistry.create()` 之后**是否还需要 `attachSession` / `indexHeader`** 才能让对话出现在工作区下
