# 上游 issue #117 · 22 项 P3 清单逐条 triage（2026-09-22）

> 对象：GitHub `Aik358/dsh-auto-memory` issue **#117**（标题：`[P3] v3.0.1 全仓只读审计遗留清单：生命周期/并发 5 项、可诊断性 6 项、结构与交付 11 项`，created 2026-09-21T13:55:40Z）。
> 清单写在 **v3.0.1（`f05de04`）时点**，本轮核验一律回 **pre 线 `D:\dsh-auto-memory` 当前代码**取证据。
> 取证口径：**只看宿主真正 import 的文件**。宿主 `lib/index.js` 的 39 条 import 全部指向 `-pre.js`（`lib/index.js:29-87`），同名 plain 文件（如 `lib/context-host.js`、`lib/python-sidecar-client.js`）是发布构建产物、mtime 停在 2026-09-07 ⇒ **改它们零运行时效果，判定「是否已修」只看 `-pre.js` 侧**。
> 因此清单里凡以 plain 文件行号给出的定位（P3-2/P3-3/P3-8/P3-12/P3-13/P3-14/P3-15/P3-19 等），**行号已整体失效**，本报告一律换算出 pre 侧的真实位置。
> 分级：本轮沿用清单自述的 P3；本报告另加 `√已修` / `✗误报` / `无法判定` 三类终态。

## 一、总表（按原清单顺序，22 行）

| ID | 位置（清单原址 → pre 线真址） | 缺陷一句话 | 当前 pre 线实测 | 证据（文件:行 + 表达式） | 严重度 | 建议动作 |
|---|---|---|---|---|---|---|
| **P3-1** | `index.js:1141/1206/1224-1226/1613` → `lib/index.js:1155/1220/1247/1238-1240/1255-1257` | ①`runtime.abortController.signal` 全仓零消费者 ⇒ dispose 后在途 await 仍继续；②置 null 的 `envelopes/segments/callLinks` 被惰性重建路径挂回已 disposed runtime | **部分修（①成立·②一半已修）**｜①**成立**：全仓 329 个 js/mjs/cjs 里 `abortController` 仅 5 处，全是构造 + `abort()`，**无一处读 `.signal`**。②**envelope/segment 侧已修**（`ingestEnvelope` 入口有 `runtime.disposed` 闸，惰性重建不可达）；**`callLinks` 侧未修**：`linkObserverCall` 无 disposed 闸 | ①`lib/index.js:1155` `abortController: new AbortController(),`；`:1220` `runtime.abortController.abort('agent disposed')`；`:1247`/`:1263` `abort('plugin disposed')`。②已修：`:1535` `if (!runtime || runtime.disposed \|\| !spec \|\| typeof spec !== 'object') return null`（挡在 `:1550` `if (!runtime.envelopes) runtime.envelopes = new ObserverRing(...)` 与 `:1607` `if (!runtime.segments) ...` 之前）；旁路闸 `:1868` `if (runtime.disposed) return null`。②未修：`:1624-1632` `linkObserverCall(...)` → `if (!runtime.callLinks) runtime.callLinks = new Map()`，函数体无 `disposed` 判断 | P3（①）／P3（②） | ①**明确不改并给理由**（AbortSignal 从来无消费者、加消费者＝改注入语义，超 P3）；②**立即修**（3 行加闸，与 `ingestEnvelope` 对齐，风险近零） |
| **P3-2** | `python-sidecar-client.js:81-91`（mtime 09-07 发布产物） → `lib/python-sidecar-client-pre.js` | respawn 先 `child=null` 再 spawn ⇒ 旧进程 `exit` 回调命中 `child!==proc` 直接 return、`rejectAll('crashed')` 不执行 ⇒ 旧代 pending 各自挂满超时（sync 帧 120s）并占死 `maxPendingRequests=64`；`stats.exits` 少计 | **已修** | 文件已由上游第五批 task-7 落地：`lib/python-sidecar-client-pre.js` **352→629 行**（`statSync` size 34519），新增 `SIDECAR_WATCHDOG_PRE_V1` 看门狗；守卫 `tests/smoke/smoke-test-sidecar-watchdog-pre.mjs`（65 项）；`dispose` 分支 `:610-615` `for (const [, entry] of [...pending]) settle(entry, { ok: false, code: 'disposed' })` 即「旧代 pending 一次性结清」的正面证据 | √ 已修 | **已覆盖**（对应 `UPSTREAM-4TH-TRIAGE-20260922.md` §5.2 #107/#108 行） |
| **P3-3** | `context-host.js:107/198`、`activation-host.js:52`、`context-host.js:583` → `context-host-pre.js:108/199/588`、`activation-host-pre.js:53` | 字符串键 Map 只在 `disposeAll` 清空、per-session dispose 路径缺位 ⇒ 长驻进程按历史会话数线性增长（对照组 `states` 用 WeakMap、`indexDegradeBySession` 有 32 上限） | **（a)(b) 未修 · (c) 已修 · (d) 误报**｜(a)`context-host-pre.js` 的 `pathsByKey` **既无 `delete` 也无 `clear`**，`disposeRuntime` 只删 WeakMap `states`；`context-host-pre` 的导出表**根本没有 `disposeSession`**。**(b)**`activation-host-pre.js` 的 registry 有真 `disposeSession`，`index.js:1233` 已接线 `_activationHost.disposeRuntime(runtime.key)` ⇒ **已修**。**(c)**`engine._lastTierQueryBySession` 有 **32 条 LRU 上限**（污染面已封顶），且**全仓零读侧消费者**。**(d)**清单称「只在 `disposeAll:818` 清空」对 (a) 准确，但把 (c) 说成「per-session 缺失」不准 | (a)`lib/context-host-pre.js:108` `const pathsByKey = new Map()`；`:199` `pathsByKey.set(String(runtimeKey \|\| ''), {...})`；`:818-822` `function disposeRuntime(runtime) { const st = states.get(runtime); if (st) st.disposed = true; states.delete(runtime) }`（**无 pathsByKey 操作**）；`:846` `for (const [, p] of pathsByKey)`（唯一读点，顺带证明它必须常驻）。**(b)**`lib/activation-host-pre.js:564-570` `disposeRuntime(runtimeKey){ ... runtimeState.delete(k); ... stepsByRuntime.delete(...) }` + `:577` `disposeSession(...)` + `lib/index.js:1233`。**(c)**`context-host-pre.js:588-594` `if (size > 32) { const oldest = keys().next().value; delete(oldest) }`。**(d)**`context-host-pre.js:100` `const states = new WeakMap()`、`:116` `const indexDegradeBySession = new Map()` + `:373-375` 同款 32 上限 | P3（(a) 真漏；增长速率 = 每会话一条小对象） | **立即修 (a)**（`disposeRuntime` 里补 `pathsByKey.delete(String(runtime.key \|\| ''))`，1 行）；(c) 排期补 per-session 出口；(b)(d) 无需动 |
| **P3-4** | `index.js:3452/3455/3460/3461` → `lib/index.js:3501/3496/3632/3506` | 接续节流的 `armed/executing/lastRunAt/rejectedEdgeAt` 是**引擎级单例** ⇒ A 会话一次 reject 压制所有会话 10 分钟、一次接续冷却所有会话 30 分钟 | **未修**（成立，但清单低估了触发面） | `lib/index.js:3493` `const st = this._autoContState \|\| (this._autoContState = {})` —— `st` 是**引擎单值**，无 per-session 维度；`:3496` `if (st.lastRunAt && now - st.lastRunAt < cooldownMin * 60000) return`；`:3501` `if (st.rejectedEdgeAt && now - st.rejectedEdgeAt < 10 * 60 * 1000) return`；`:3502` `if (st.armed && Date.now() < st.armed.expiresAt) return`；`:3503` `if (st.executing) return`；`:3506-3513` `st.armed = { edgeAt, sessionId: sid, ... }` —— **`sessionId` 只存不判**，任何会话走到这里都会覆盖它 | P3（多会话用户可感知：另一个会话的水位接续被静默跳过） | **排期**（改法是把 `_autoContState` 拆成 `Map<sessionId, st>`，但牵动 `:3655-3690` 的 `autoContinueView` 投影与既有断言 ⇒ 需连带改守卫，不是一行） |
| **P3-5** | `index.js:5955-5958 appendText` → `lib/index.js:5974/5996-5999` | 非 anchor 分支是跨 await 的 read-modify-write；`memoryAnchorEnabled` 默认 false ⇒ 默认就走这条路 | **未修（但无法判定当下可触发）** | `lib/index.js:337` `memoryAnchorEnabled: false,`（默认值未改）；`:5965` `if (this.config.memoryAnchorEnabled !== true) return null` ⇒ `docStore` 为 null ⇒ 走 `:5996-5999` `const existing = await this.readTextSafe(p)` → `const body = existing ? ... : text` → `await mkdir(...)` → `await writeFile(p, body, 'utf8')`。**同一写入源并发**：未能构造（清单自述「列为待观察而非缺陷」，本轮同样只做了静态核对，未做并发探针） | P3（清单诚实标注为低） | **明确不改（本轮）+ 标注为待观察**：改写为「追加语义（appendFile/带锁）」会动写入原语契约（`memoryWriteError` 的「失败即抛」），且当前无已知触发场景 ⇒ 只在白板记一条观察项 |
| **P3-6** | `index.js:11506` → `lib/index.js:11628` | `const q = …reduce(...)` 已是数字，`queueLength: q.length` 恒 `undefined` ⇒ JSON 静默丢键 | **未修（成立，且比清单更严重）** | `lib/index.js:11621` `const q = engine.runtimes.values().reduce((sum, rt) => sum + rt.pendingConsolidations.length, 0)`（数字）；`:11628` `queueLength: q.length,` ⇒ `(3).length === undefined` ⇒ `JSON.stringify` 丢键。**加重项**：全仓 `polling-heartbeat` 仅命中 `lib/index.js:6872/11618/11622` 三处（都是代码自身），**`docs/` 与两份 README 一处未提** ⇒ 清单说它是「手册让运维者确认轮询活着的唯一探针」，实测**手册根本没写这个文件** | P3（一行修） | **立即修**（`q.length` → `q`；顺带在 HANDBOOK 补一句该探针的位置与字段，否则修了也没人知道看哪） |
| **P3-7** | `index.js:9070-9085` → `lib/index.js:9140-9156` | `_dshAutoMemoryRejectionStat` 只挂 `process`，`debugInfo` 与所有端点都不读；唯一读者是断言源码字符串的套件；但两份文档已记为「✅ 可观测」 | **未修（成立）** | `lib/index.js:9147` `process._dshAutoMemoryRejectionStat = rejStat`（写点，全仓唯一）；**全仓无读点**；唯一读者 `tests/smoke/smoke-test-t9-diag-home-pre.mjs:35` `assert(/_dshAutoMemoryRejectionStat/.test(IDX), ...)`、`:43` `assert(/process\._dshAutoMemoryRejectionStat = rejStat/.test(IDX), ...)` ⇒ **纯源码文本断言，只验「它被写出来了」，不验「谁能读到」**。文件外命中：`docs/internal/PRE-FRONTEND-CHECKLIST-20260919.md:667` | P3（文档与实现不一致，属「可观测性承诺未兑现」） | **立即修（选一刀）**：要么把 `rejStat` 塞进 `debugInfo`/状态端点（真可观测），要么把两份文档的「✅ 可观测」降级为「仅计数不暴露」 |
| **P3-8** | `index.js:906-914` 与 `context-host.js:39-52` → `lib/index.js:928-934`、`lib/context-host-pre.js:40-49` | 两套并行 diag 机制；无级别体系，详略只能靠 4 个**任何文档都没写**的 env 闸；diag 文件无轮转/体积上限 | **未修（成立）** | 机制一 `lib/index.js:928-931` `_diagChain = _diagChain.then(() => appendFile(path.join(dshHome(), 'dsh-auto-memory-pre-diagnose.log'), line, 'utf8'))` + `:932` `console.log('[dsh-auto-memory-pre] ' + msg)`（**文件 + console 双写**）。机制二 `lib/context-host-pre.js:49` `appendFileSync(path.join(base, 'dsh-auto-memory-pre-diagnose.log'), ...)`（**只写文件**，同文件另一路径）。四个 env 闸全仓命中：`DSH_CTX_DEBUG`→`context-host-pre.js:679/690/694`、`DSH_ACT_DEBUG`→`activation-host-pre.js:438`、`DSH_F1_DEBUG`→`index.js:5975`、`DAM_PROBE_STACK`→`index.js:2144`；**`docs/**.md` + `README.md` + `README.zh-CN.md` 对这四个名字命中数 = 0**（实测 grep 输出为空）。轮转：`*-diagnose.log` 只有 append、**无体积上限**；唯一有界的是 shadow-decide 侧 `context-host-pre.js:777` `writeFileSync(f, lines.slice(-JSDECIDE_SHADOW_MAX_LINES).join('\n') + '\n')` | P3（运维面：长了没法读、响了没法关） | **排期**（做一半：①四个 env 闸写进 HANDBOOK；②`*-diagnose.log` 加上限/按日切分。级别体系可延后） |
| **P3-9** | `index.js:6218/6257/6265`、`shadow-host.js:228`、`evidence-store.js:143` → `index.js:6259/6298/6311`、`shadow-host-pre.js:228`、`evidence-store-pre.js:143` | 热路径用裸 `diag`/`console.error` 未节流（`diagThrottled` 只覆盖 `restoreLastAgent:7222-7261`）⇒ 持续性故障每轮 recall 双写。这正是 `index.js:836-841` 自述「打开就一直弹」事故的复发面 | **未修（成立，且未节流面远大于清单所列 5 处）** | 三处点名位置全部对上：`lib/index.js:6259` `try { diag('recall 语义臂择优降级: ' + ...) } catch (_) {}`、`:6298` `diag('evidence-agg 降级为中性(impMap 空): ' + ...)`、`:6311` `diag('temporal-parse 降级(无时间臂): ' + ...)`；`lib/shadow-host-pre.js:228` `if (!paths) { console.error('[shadow-diag] no-paths'); ... }`；`lib/evidence-store-pre.js:143` `console.error('[evidence-store] write-failed: ' + this.stats.lastWriteError)`。节流函数 `:864-872` `function diagThrottled(key, msg, windowMs)` 全仓调用点仅 `:7299/7301/7302/7319/7336/7338`（全在 `restoreLastAgent` 内）+ `:9222`（`hubIo` onError）；**裸 `diag(` 在 `index.js` 有数十处**（含 `:5965` 附近的 turn-stopping 主路径与 recall 主路径） | P3（上一轮同源事故的实际复发面） | **立即修（最小面）**：只把 recall 主路径三处 + shadow/evidence 三处换成 `diagThrottled(key, ...)`，共 6 个调用点；**不做全仓替换**（会静默掉真正一次性事件） |
| **P3-10** | `index.js:10443-11083`（25+ 处）、`:10807/10875/10937/10971` → `lib/index.js:10551-11621` 的 routes 数组 | 路由 catch 只回 500 不留服务端日志；`String(e && e.message)` 在非 Error 抛出时响应为 `"undefined"` | **未修（成立；实测结构：49 处带留痕 / 29 处静默）** | routes 数组实测边界 `lib/index.js:10551` `const routes = [` → `:11621` `]`，数组内 **49 个 handler**；数组内 catch 点 **49 处有 `diag(/console./writeJson` 留痕、29 处静默**（样例 `:10627` `} catch (_) {}`、`:10642` `} catch (_) {}`）。取反例：`lib/index.js:11362` `} catch (e) { writeJson(res, 500, { error: String(e && e.message ? e.message : e) }) }` —— **这一处已做了非 Error 兜底**，说明口径在文件内已不一致 | P3（排障面） | **排期**（抽一个 `routeCatch(tag)(e,res)` 统一：留 `diagThrottled` 一条 + 统一 `String(e && e.message \|\| e)`；29 处机械替换，风险低但要连带看断言） |
| **P3-11** | `index.js:9924-9930`、`disposeSession:1257-1263`、`dispose:1200-1226` → `lib/index.js:1262-1278` 等 | 会话释放全空 catch 或静默 `false` ⇒ 泄漏只能靠 `debugInfo` 的 runtimes 列表间接发现 | **未修（成立）** | `lib/index.js:1271-1278` `disposeSession(session) { try { ... return this.dispose(runtime.agent) } catch (e) { return false } }` —— **catch 全空**；`:1216-1218` `dispose(agent) { ... if (!runtime \|\| runtime === this._default) return false ... }` —— 三种失败原因（无 agent/无 runtime/default）**都只回 `false`，不留痕**；间接可见性确在 `:6988-6991`（`debugInfo.runtimes` 逐会话投影） | P3（同 P3-10，属排障面） | **排期**（并入 P3-10 的统一留痕改造，避免两次动 `index.js`） |
| **P3-12** | `config-io.js:72/92` ↔ `fs-retry.js:28` → `lib/config-io-pre.js:72/92/97`、`lib/fs-retry-pre.js:28` | 原子写三套并行；四处手写 tmp→rename 绕过；为 issue #48 而生的 `retryRename` 只接了 3 处 ⇒ `config.json` 保存路径仍走裸 `renameSync`，失败只回 `{ok:false}` | **未修（成立，三处 renameSync 一处不少）** | `lib/config-io-pre.js:17`（自述「写 tmp → `renameSync` 覆盖目标」）、`:61` `renameSync(file, finalPath)`、`:77` `renameSync(tmp, file)`（sync 原子写）、`:97` `renameSync(tmp, file)`（async 原子写）—— **全是裸 `renameSync`，无退避**。`retryRename` 定义 `lib/fs-retry-pre.js:28`，`RENAME_RETRY_DELAYS = [0,50,150,400,1000]`、`TRANSIENT_RENAME_CODES = {EPERM,EACCES,EBUSY}`；接线点仅 3 处：`lib/index.js:2533`、`:2565`（白板索引重建，上一轮 #110/看板原子性修复）、`lib/memory-writer-pre.js:258`、`lib/subagent-gc-pre.js:264`。配置保存路径确认走裸 rename：`lib/index.js:2033` `await writeTextAtomicPre(this._configPath, ...)`、`:2053`/`:10841` `writeTextAtomicPreSync(this._configPath, ...)` | P3（Windows 上「设置页保存失败但只回 ok:false」的已知族） | **立即修（只补配置保存这一条链）**：给 `config-io-pre.js` 的 async 版换 `await retryRename(tmp, file)`（`retryRename` 本就是 async，签名直接兼容）；**sync 版不动**（`retryRename` 是 async，改 sync 会破 `writeTextAtomicPreSync` 的同步契约 —— 这正是清单没说的约束） |
| **P3-13** | `index.js:6782 memory/degrade-pre/`、`shadow-host.js:128 memory/retrieval-pre/`、`python-sidecar-client.js:342 kind:'python-sidecar-pre'` → `index.js`（`degrade-pre` 目录）、`shadow-host-pre.js:128`、`python-sidecar-client-pre.js:144/618` | `-pre` 残留超出「仅保留在测试文件名/端点前缀/工具名」的既定口径，且落在**磁盘数据目录与身份串**上（改名会牵动用户已有数据，需要显式决策） | **未修（成立，三条全部还在）** | ①`lib/shadow-host-pre.js:128` `function auditDir() { return path.join(dshHome(), 'memory', 'retrieval-pre', 'audit') }`（另见 `:6` 文件头自述）—— **落盘目录**；②`lib/python-sidecar-client-pre.js:144` `const rec = { at, level, sidecar: 'python-sidecar-pre', generation, message }`、`:618` `kind: 'python-sidecar-pre',` —— **身份串**；③`memory/degrade-pre/` 目录：pre 源码内未出现字面量，但有两条守卫把它当**冻结事实**断言：`tests/smoke/smoke-test-r3-degrade-pre.mjs:240` `'★ 落盘路径 = <dshHome>/memory/degrade-pre/latest.json'`、`tests/smoke/smoke-test-issue111-docs-real-names-pre.mjs:111` `ok(hb.includes('memory/degrade-pre/'), 'HANDBOOK: 显式记录了 memory/degrade-pre/ 这条例外')` ⇒ **改名要同时改守卫与 HANDBOOK，且用户的既有目录要迁移** | P3（**须用户裁定**，非技术难度问题） | **排期（等裁定）**：本报告不建议自行改名 —— 它是磁盘路径 + 身份串，属「会牵动用户已有数据」的显式决策点（清单自己也这么定级） |
| **P3-14** | `semantic-js.js:79/95/183` → `lib/semantic-js-pre.js:79/95/183` | 把开发树 `../artifacts/m7-live-pre/js-semantic-trial/*`（`.gitignore` 排除、纯维护者机器路径）作为**生产**模型/peer 探测路径打进发布包 | **未修（成立，3 处全在 pre 线，且会进发布包）** | `lib/semantic-js-pre.js:79` `path.join(pluginDir, '..', 'artifacts', 'm7-live-pre', 'js-semantic-trial', 'models')`、`:90` 注释「4) 开发树 artifacts」、`:95` `.../js-semantic-trial/node_modules/@huggingface/transformers`、`:183` `.../js-semantic-trial/models/${E5_MODELS_SUBDIR}/onnx/model_quantized.onnx`。**会进发布包**：`tools/release.mjs:234` 的转换表把 `semantic-js-pre.js` 列为要转成发布名的文件之一（同批 `recall-fusion-pre.js`/`semantic-decide-pre.js`）⇒ `-pre` 内容就是发布内容。plain 侧同样存在（`lib/semantic-js.js:73/89/164`），说明它不是 pre 线新增的漂移 | P3（用户机上恒为不存在的路径；实际影响限于「探测候选里多一条死路径」） | **立即修（低成本高确定性）**：把第 4 条候选改成 `process.env.DAM_DEV_TREE === '1'` 才纳入（或直接删）—— 删掉后用户侧行为零变化（该路径在用户机上本就不存在），维护者侧用 env 闸保命 |
| **P3-15** | `lib/acceptance.js`（71 行） | 孤儿模块：零 lib 侧引用，只被 `smoke-test-p5-acceptance-pre.mjs` 使用 ⇒ 一道发布门被随包发布却从未接线 | **✗ 误报（该文件在 pre 线不存在）** | `lib/acceptance.js` **`existsSync` = false**（实测）。pre 线存在的是 `lib/acceptance-pre.js`（71 行，导出 `ACCEPTANCE_VERSION = 'acceptance_pre_v1'` / `recordAcceptancePre` / `acceptanceSummaryPre`），读者只有 `tests/smoke/smoke-test-p5-acceptance-pre.mjs:21/30/40/52/61/65/72/85`。全仓（排除 node_modules/.git/artifacts）对 `acceptance.js` 的命中 = **0**（除本报告与上游 issue 文本本身） | ✗ 误报 | **明确不改 + 反证**：清单按 `main` 线写，而 pre 线该文件不存在；「随包发布却从未接线」在当前树上无从成立（文件都没有）。**若上游指的是 `acceptance-pre.js`**，则「零 lib 侧引用」成立，但它是 `tests/smoke/smoke-test-p5-acceptance-pre.mjs` 的被测对象 ⇒ **它是一道被测的门，不是发布门**，不构成缺陷 |
| **P3-16** | 全仓 19 个零引用 export；例 `tier-layer-inject.js:115 isCurrentStatusPre = isCurrentPre`、`m7-wire.js:207 validateIndexAckPayloadPre` | 零引用导出（含纯别名、唯一未接线的校验器） | **未逐条核（抽样两例成立）** | 例一 `lib/tier-layer-inject-pre.js:115`（文件内命中仅此 1 处）—— 纯别名，全仓无第二处引用。例二 `lib/m7-wire-pre.js:207`（全仓命中仅 `lib/m7-wire.js:206` 的对位副本）—— `index-sync` 侧确实改用手写判据（见清单所述 `accepted !== true`）。**其余 17 个未逐条清点**（清单未给名单） | P3（清理项） | **排期**（要动手先要一份完整名单；清单只给了 2 个例子，无法据此开工） |
| **P3-17** | `package.json:50-51` ↔ `lib/client.js:4909/5141` → `package.json`（`optionalDependencies`）、`lib/client.js:5051/5283/5906` | UI 引导命令 `pnpm add @huggingface/transformers` 会装到 4.3.0，超出 `optionalDependencies: ^3.7.6`；`resolvePeerTransformersDir` 与深扫热接入**只判存在性、完全不校验版本** | **未修（成立；且清单漏说「声明本身不被 npm 兑现」的原因）** | `package.json` `optionalDependencies = {"@huggingface/transformers":"^3.7.6"}`、`version = 3.0.1`。UI 引导命令**无版本号**：`lib/client.js:5283` `pnpm approve-builds\npnpm add @huggingface/transformers\n# 然后重启 dsh web`（同族文案 `:5051`/`5895`/`5906`）。版本校验缺位：`lib/semantic-js-pre.js:106` `resolvePeerTransformersDir(pluginDir, extraDirs)` 全函数只有 `existsSync`/`req.resolve`，**无 `readFileSync(pkg.json).version` 读取、无 semver 比较**；`:204` `return await import('@huggingface/transformers')`（动态 import，无版本前置）。**漏说点**：`optionalDependencies` 对「未被 import 的包」不会安装 ⇒ 该声明在本项目安装模型里既不被 npm 兑现、也不被代码校验 | P3（跨大版本 API 漂移会表现为「引擎静默失效」） | **排期**（两件小事：①UI 命令钉版本；②`resolvePeerTransformersDir` 命中后读一次 `package.json.version`，落在 `^3.x` 之外记一条 `[降级]` 而非静默接入） |
| **P3-18** | `tests/m7-2-fixtures/embedding-fixture.json` | 金标 fixture 自述 `chunkingPolicyVersion: "m7_chunk_pre_v1"`，代码常量是 `m7_chunk_v1` ⇒ 谱系标注与冻结策略名不一致（`smoke-test-m72-pre.mjs:28` 因此常红） | **未修（成立；但「常红」这一因果不成立）** | fixture 里该字段**不在顶层**而在 `policy` 对象内：`tests/m7-2-fixtures/embedding-fixture.json:15`（`"chunkingPolicyVersion": "m7_chunk_pre_v1"`，与 `:96-100` 的 `policy{name:"para-512-noov", chunkingPolicyVersion, params}` 同块）。代码权威常量：`python/m7_embedding_v1.py:9` 注释 + `:31` `CHUNK_POLICY_VERSION = 'm7_chunk_v1'`。**生产方是导出脚本**：`python/bench/m7b_export_fixture.py:98` `"chunkingPolicyVersion": "m7_chunk_pre_v1",` —— **硬编码**，与 `:31` 常量无绑定关系。**「因此常红」不成立**：`tests/smoke/smoke-test-m72-pre.mjs:28` 断言的是 `fx.policy.chunkingPolicyVersion === 'm7_chunk_pre_v1'`，与 fixture 现值**一致** ⇒ 套件在这条上不会红；真正的漂移是「fixture 的标注 ≠ python 常量」 | P3（文档/谱系一致性问题，非功能缺陷） | **明确不改并给理由（本轮）**：①当前无任何代码读该字段做决策（只被套件自证）；②要改必须**重导 fixture**（`.py` 是生产方），而重导会连带 `configHash` 与全部向量，属不可逆数据操作 ⇒ 与 `D 批（碰数据需先备份）`同批处理 |
| **P3-19** | `lib/index.js` 结构 | `const routes = [`（`:10429-11493`，1065 行）在一条数组字面量里内联全部 51 个 handler；`checkWaterLevel :3006-3253`(248L)、`context-host.js:297 onSegmentAccepted`(297-610, 314L)、`renderMemoryDynamic :5531-5759`；另 `:958` 注释自述「14 个 `_pre` 工具、24 条路由」与实测 17/49 已失真 | **未修（成立；实测四项数据、一处漂移已定位）** | ①routes 数组实测 `lib/index.js:10551-11621` = **1071 行**，数组内 **49 个 handler**（原判 1065 行/51 个，差 2）。②`lib/index.js:3047-3294` `async checkWaterLevel(agent)` = **248 行**（与清单一致）。③`lib/context-host-pre.js:298-616` `onSegmentAccepted(runtime, seg, envelope)` = **319 行**。④`lib/index.js:5572-5800` `renderMemoryDynamic(context)` = **229 行**。⑤失真的注释实测在 `lib/index.js:978`：`// model-visible prompt、14 个 _pre 工具、24 条路由、持久 Markdown 与观察账本存储。` —— 实测 **17 个 `defineTool(`**（`:10189`–`:10545`）、**49 条路由** ⇒ 确认失真 | P3（可读性/维护性） | **排期（只做零风险的一刀）**：先把 `:978` 的「14 个 / 24 条」改成「17 个 / 49 条」（或改成不带数字的表述，避免再漂）；**函数拆分不动**（`checkWaterLevel`/`onSegmentAccepted`/`routes` 都是热路径 + 大量断言依赖当前形状） |
| **P3-20** | `.github/cloud/qq-webhook/index.js:421-425`、`lib/index.js:11214-11227`、`lib/semantic-js.js:445-478` → `.github/cloud/qq-webhook/index.js:556`、`lib/index.js:11339-11356`、`lib/semantic-js-pre.js` | 三处边界缺失：webhook 无请求体上限；设置端点仅按键名白名单过滤、`patch[key]=body[key]` 无类型/路径校验（键含 `memoryRoot`/`userMemoryDir`）；模型下载流无字节上限 | **三项中第 1 项已修、第 2 项未修、第 3 项未核** | ①**已修**：`.github/cloud/qq-webhook/index.js:556` `const MAX_BODY_BYTES = envNum(process.env.MAX_BODY_BYTES, 1024 * 1024) // 入站 body 上限(issue #116)`、`:561-568` `req.on('data', ...)` 内 `if (received > MAX_BODY_BYTES) { chunks.length = 0; noteReject('body_too_large', ...) }`（文件 mtime `2026-09-21T16:43`，属第五批修复，git 状态 ` M`）。②**未修**：`lib/index.js:11342` `const allowed = Object.keys(DEFAULT_CONFIG)`、`:11344` `for (const key of allowed) if (body[key] !== undefined) {`、`:11355` `patch[key] = body[key]` —— 只对 `semanticEngineMode`(`:11346` 枚举白名单) 与 `injectExcludeSources`(`:11349-11354` 数组/元素校验) 两个键做了类型判据，**其余键裸赋值**；而 `:272` `memoryRoot: '~/.dsh/memory/workspaces'`、`:268` `userMemoryDir: '~/.dsh/memory'` 确实在 `DEFAULT_CONFIG` 键集内 ⇒ 一次 POST 可改记忆落点。③下载流字节上限**未核**（本轮未读 `semantic-js-pre.js:445-478` 对位段落） | P3（②限于 loopback + GUI 同信任域，属「**配额/护栏**」而非提权） | **立即修（只修 ② 的路径类键）**：给 `memoryRoot`/`userMemoryDir` 加「必须是非空字符串 + 必须落在 `dshHome()` 之下」判据（与 `injectExcludeSources` 同款 fail-closed 丢弃）→ 与「记忆落点」这条用户可感知面直接相关；①已覆盖、③留待核 |
| **P3-21** | `semantic-decide.js:235-237` ↔ `python/m7_activation_features_v2.py:337` → `lib/semantic-decide-pre.js:235-237` | `proactive_margin_fallback` 分支双侧同时不可达：要求 `intent < 0.35`，而所在 lane 门为 `tauLane = 0.45` ⇒ 文档承诺的「无 hit 时转 proactive」从未生效（不是 JS/Python 漂移，是共同的死分支） | **未修（成立；且可证「即使 tauLane 调低也仍不可达」⇒ 恒死而非条件死）** | JS：`lib/semantic-decide-pre.js:194` `return intentProb >= (th.tauLane \|\| 0) ? 'explicit' : 'proactive'`（lane 门）→ `:218` `const lane = computeLane(intent, policy)` → `:228` `if (lane === 'explicit') {` → `:235-237` `if (margin >= deltaPro && nCand >= 2 && intent < 0.35 && denseTop < denseTopArm) return finish('prefetch', ['proactive_margin_fallback'])`。policy：`python/policies/activation_policy_v2.json` 的 `thresholds = { tauLane: 0.45, tauHi: 0.45, tauLo: 0.35, deltaExp: 0.03, deltaPro: 0.05 }`。Python 对位：`python/m7_activation_features_v2.py:226` `return 'explicit' if intent_prob >= policy['thresholds']['tauLane'] else 'proactive'` + `:335-337` 同款条件。**「恒死」推演（推断，逻辑推演非实测）**：进 `explicit` lane ⇒ `intent >= tauLane`；即便把 `tauLane` 调到 0.2，`intent ∈ [0.2, 0.35)` 时会先被 `:234` `if (intent >= (th.tauLo \|\| 0) && hit) return finish('prefetch', ['explicit_lane_weak'])` 命中（`hit` 为该 lane 前置条件）⇒ `:235` 分支仍拿不到执行 | P3（文档承诺 vs 实现不符；影响面限于该 reason code 从未出现） | **排期（等一次判定）**：要么删掉该死分支 + 同步改文档承诺，要么把 `intent < 0.35` 改成 `intent < th.tauLane` 让它真能走到 —— **这是产品语义二选一，不是纯修 bug** |
| **P3-22** | `semantic-decide.js:36-72` → `lib/semantic-decide-pre.js:45-62/64-73` | issue #68 已让 JS `normalizeText` 逐码点复刻 Python，但 Python `isalnum()` 覆盖全 Unicode，JS 侧只覆盖 ASCII/CJK/全角/Latin-1/Latin Ext-A ⇒ 西里尔/希腊/阿拉伯/CJK 扩展 B 文本仍会分叉 | **未修（成立，覆盖表逐条对上清单描述）** | `lib/semantic-decide-pre.js:64-73` `function normalizeText(text) { const lower = String(text).toLowerCase(); for (const ch of lower) { const cp = ch.codePointAt(0); if (isAlnumPythonish(cp)) out += ch } }`；`:45-62` `isAlnumPythonish(cp)` 的判据集合 **恰好 6 族**：`0x30-0x39`(ASCII 数字)、`0x61-0x7a`/`0x41-0x5a`(ASCII 字母)、`0x4e00-0x9fff`(CJK 基本区)、`0xff10-0xff19`(全角数字)、`0xff21-0xff3a`/`0xff41-0xff5a`(全角字母)、`0x00c0-0x00ff`(Latin-1 去 `0x00d7`/`0x00f7`)、`0x0100-0x017f`(Latin Ext-A) —— **无西里尔(0x0400-)、希腊(0x0370-)、阿拉伯(0x0600-)、CJK 扩展 B(0x20000-)** ⇒ 清单描述与实际覆盖表**逐条吻合** | P3（清单自述「影响面小，如实定级」，同意） | **明确不改并给理由**：①分叉只在「同一输入在两引擎下取不同 gram」时发生，且影响面限 `normalizeText` 的消费者（intent/echo 特征），不影响记忆读写；②要真对齐得引 Unicode 属性表（`\p{Alphabetic}` + Python 语义差异还需逐条对），属跨引擎契约级改动 ⇒ **应并入「JS/Python 两引擎对齐」这条既有议题统一裁定**，不单独立项 |

> 表内「实测」列的四种终态计数：**√已修 1**（P3-2）／**✗误报 1**（P3-15，另有 P3-3(c)/P3-20① 两个子项为误报或已修）／**部分修 1**（P3-1）／**未修（成立）18**（其中 P3-5、P3-16、P3-20③ 三项为「未逐条核/无法判定」）。

---

## 二、分组小结

### 2.1 立即修（值得占用下一轮）—— 7 条

| ID | 为什么值得现在做 | 改动面 | 需否新守卫 |
|---|---|---|---|
| **P3-6** | **一行修 + 直击用户可感知面**。`polling-heartbeat.json` 是运维者判断「轮询还活着吗」的探针，而 `queueLength` 恒 `undefined` ⇒ 该字段**从来没写出来过**。且实测「手册根本没提这个文件」⇒ 修字段的同时必须补文档，否则修了也没人知道看哪 | `lib/index.js:11628` 一行 + `docs/HANDBOOK.md` 一句 | 建议加（心跳 JSON 必含数值型 `queueLength`） |
| **P3-1（②`callLinks` 子项）** | **3 行加闸，与同文件已有防线对齐**。`ingestEnvelope:1535` 已用 `runtime.disposed` 挡住 envelope/segment 的重建，唯独 `linkObserverCall:1624` 漏了 ⇒ 属「同一防线漏一个入口」 | `lib/index.js:1624`（`if (!runtime \|\| !callId) return` 处补 `\|\| runtime.disposed`） | 建议加（复用 `smoke-test-context-observer.mjs` 的 P3f dispose 段形状） |
| **P3-3(a)** | **1 行修 + 真泄漏**。`context-host-pre.js` 的 `pathsByKey` 是模块级 Map、存**字符串键**，全文件无 `delete`/`clear`，且该 host 连 `disposeSession` 都没导出 ⇒ 长驻进程按历史会话数**单调增长**。对照 `activation-host-pre.js` 已于 issue#58 修好同款 ⇒ 属「同一 bug 的两份实现只修了一份」 | `lib/context-host-pre.js:818-822`（`disposeRuntime` 内补 `pathsByKey.delete(String(runtime.key \|\| ''))`） | 建议加（源码级断言：`disposeRuntime` 体内必须出现 `pathsByKey.delete`） |
| **P3-12（仅配置保存链）** | **1 行修 + 有真实故障族背书**。`retryRename` 本就是为 issue #48（Windows EPERM/EACCES/EBUSY）造的，却唯独没接到「设置页保存 `config.json`」这条**用户最常直接触发**的链上 ⇒ 保存失败只回 `{ok:false}`。注意：**只改 async 版** —— `writeTextAtomicPreSync` 是同步签名，不能硬塞 async 的 `retryRename` | `lib/config-io-pre.js:92-99`（`renameSync(tmp, file)` → `await retryRename(tmp, file)` + import） | 建议加（沿用 `smoke-test-issue48.mjs` 的故障注入形状） |
| **P3-14** | **零风险 + 带发布影响**。开发树路径经 `tools/release.mjs:234` 的转换表**确实会进发布包**，而在用户机上它恒为不存在的路径。删掉后**用户侧行为零变化**（本就不存在），维护者侧用 env 闸保命 ⇒ 性价比极高 | `lib/semantic-js-pre.js:79/95/183` 三处候选 | 建议加（断言发布转换后的文件里不再含 `m7-live-pre`） |
| **P3-9（仅 6 个调用点）** | **上一轮同源事故的复发面**。`index.js:856-872` 自述的事故就是「每 tick 一条 ⇒ 用户看到『打开就一直弹』」，而 recall 主路径三处 + shadow/evidence 两处仍是裸 `diag`/`console.error` ⇒ 持续性故障按**每轮**频率双写。只改这 6 点，**不做全仓替换** | `lib/index.js:6259/6298/6311`、`lib/shadow-host-pre.js:228`、`lib/evidence-store-pre.js:143` | 建议加（断言这几个 key 走 `diagThrottled`） |
| **P3-20（仅②的路径类键）** | **改的是「记忆落点」这条用户可感知面**。设置端点目前只对 `semanticEngineMode` 与 `injectExcludeSources` 两个键做了类型判据，其余裸赋值，而 `memoryRoot`/`userMemoryDir` 就在键集内 ⇒ 一次 POST 可把后续 append/rewrite 落点移出记忆区 | `lib/index.js:11342-11355`（循环内加两个路径类键的 fail-closed 判据） | 建议加（断言 `memoryRoot` 传非字符串/越界路径时被丢弃） |

**为什么是这 7 条**：五条是「1–3 行改 + 有明确断言形状 + 现有守卫可直接复用」；两条（P3-6 / P3-20②）命中的是**用户能直接感知**的面（运维探针读不到 / 记忆落点可被改）。七条对 `lib/index.js` 的净改动 **< 20 行**，且**互不重叠**（不同函数、不同行段）⇒ 可在同一批次内串行完成、共用一次全量回归。

### 2.2 可延后 —— 11 项（含 9 条整项 + 2 个子项）

| ID | 延后理由 |
|---|---|
| **P3-4** | 成立且用户可感知（多会话下接续被静默跳过），但改法牵动 `:3655-3690` 的 `autoContinueView` 投影 + 既有断言 ⇒ 属「要连带改守卫」的中等改动，须整块排期而非顺手 |
| **P3-7** | 真实缺口（计数写了没人读、两份文档却记「✅ 可观测」），但两条修法（接进 `debugInfo` / 降级文档）都要先定「谁该看到」⇒ 是文档口径题，跟着 `task-8 docs-hermetic` 那条线走更省 |
| **P3-8** | 成立（4 个 env 闸全仓零文档 + 日志无上限），但「级别体系」是大改；可先只做一半：env 闸写进 HANDBOOK + `*-diagnose.log` 加体积上限 |
| **P3-10** | 成立且量大（routes 数组内 49 处留痕 / 29 处静默），但抽 `routeCatch` 是 29 处机械替换 + 需连带看断言 ⇒ 与 P3-11 同批做 |
| **P3-11** | 与 P3-10 同类同源（排障留痕），且「静默 `false`」三条分支**是否该留痕需先定**（`dispose` 的 `false` 有正常语义：无 agent / 无 runtime / 是 default）⇒ 并入 P3-10 统一裁定 |
| **P3-13** | **须用户裁定**：改名牵动磁盘数据目录（`memory/degrade-pre/`、`memory/retrieval-pre/`）与身份串（`python-sidecar-pre`），且两条守卫把它当**冻结事实**断言、HANDBOOK 显式记录了该例外 ⇒ 不是技术问题 |
| **P3-16** | 需先拿到「19 个零引用 export」的**完整名单**才能开工（清单只给了 2 个例子；本轮抽样 2 例成立）⇒ 前置是清单，不是代码 |
| **P3-17** | 成立，影响面限于语义引擎的跨大版本漂移（表现为**静默失效**而非崩溃），要动 UI 文案 + 探测函数两处 ⇒ 中等优先级 |
| **P3-19** | 成立（实测 1071 行 / 49 handler / 248L / 319L / 229L / 失真的 `:978` 注释），但函数拆分风险高（热路径 + 大量断言依赖当前形状）⇒ **建议只做零风险那一刀**：把 `:978` 的「14 个 _pre 工具、24 条路由」改成实测的 17 / 49（或改成不带数字的表述，避免再漂） |
| **P3-21** | 成立且可证「恒死」（不是条件死），但修法要先做**产品二选一**：删死分支 + 改文档承诺 **vs** 放宽条件让它真生效 ⇒ 属裁定题 |
| **P3-22** | 成立但清单自述「影响面小，如实定级」；真对齐要引 Unicode 属性表 + 逐条对 Python 语义 ⇒ 应并入「JS/Python 两引擎独立可互换」这条既有铁律议题统一处理，不单独立项 |
| **P3-3(c)**（子项） | 缓冲已封顶（`_lastTierQueryBySession` 有 32 条 LRU 上限）⇒ 不是增长缺陷；剩余问题只是「无 per-session 出口 + 零读侧消费者」，可与 P3-16 同批做死码清理 |
| **P3-20③**（子项） | 「模型下载流无字节上限」本轮**未核**（未读 `semantic-js-pre.js:445-478` 对位段落）⇒ 先核再排，不进立即修批次 |

### 2.3 判定不改 —— 5 条（逐条理由）

1. **P3-1（①`abortController.signal` 零消费者）** —— **不改**。
   实测全仓 329 个 js/mjs/cjs 里 `abortController` 只有 5 处，全是构造 + `abort(...)`，确实没有读 `.signal` 的地方。但清单说「⇒ dispose 后在途 await 仍可继续」这句是**静态推演**（清单自己就标注「后果为静态推演」）。要真让它生效，必须给全部在途 await 加 signal 消费点 ⇒ 那是**改注入/写入语义**，不是修缺陷，超 P3 范围。
   **处置：明确不改 + 给理由；在 issue 回帖里写明「已核为真、按设计不改」**，避免被读成漏项。

2. **P3-5（`appendText` 非 anchor 分支跨 await RMW）** —— **本轮不改**。
   代码事实成立（`:5996` `await readTextSafe` → `:5999` `await writeFile`，且 `:337` `memoryAnchorEnabled: false` 是默认值 ⇒ 默认就走这条路），但**未能构造出可触发场景**：同文件写者目前都走 `await` 单链的 maintain / hubFlush 串行门。改法（`appendFile` 或加锁）会动写入原语「失败即抛」的既有契约 ⇒ 收益不明、风险确定。
   **处置：明确不改（本轮）+ 白板记一条观察项**；若将来出现双会话同文件交错的实机证据再立项。

3. **P3-15（孤儿模块 `lib/acceptance.js`）** —— **✗ 误报，不改 + 反证**。
   实测 `lib/acceptance.js` **不存在**（`existsSync` = false）；pre 线只有 `lib/acceptance-pre.js`，且它也**不是**零引用（`tests/smoke/smoke-test-p5-acceptance-pre.mjs:21-85` 多处在用）。清单是按 `main` 线写的，当前树上「随包发布却从未接线」**无从成立**（文件都没有）。
   **反证**：全仓（排除 `node_modules`/`.git`/`artifacts`）对字符串 `acceptance.js` 的命中 = **0**（除本报告与上游 issue 文本自身）。
   **若上游本意是 `acceptance-pre.js`**：它确实零 lib 侧引用，但它是**被测对象**（`smoke-test-p5-acceptance-pre.mjs` 的被测件），不是一条「当门用的发布门」⇒ 不构成缺陷。

4. **P3-18（fixture 谱系标注不一致）** —— **本轮不改 + 理由**。两条实测修正清单：
   - 该字段**不在顶层**，在 `policy` 对象内（`tests/m7-2-fixtures/embedding-fixture.json:15` 与 `:96-100` 同块）；
   - 「`smoke-test-m72-pre.mjs:28` 因此常红」**不成立** —— 套件断言的正是 `=== 'm7_chunk_pre_v1'`，与 fixture 现值**一致**，这条不会红。

   真正的漂移是「fixture 标注 ≠ `python/m7_embedding_v1.py:31` 的 `CHUNK_POLICY_VERSION = 'm7_chunk_v1'`」，根因在导出脚本 `python/bench/m7b_export_fixture.py:98` **硬编码**了 `m7_chunk_pre_v1`（与常量无绑定）。
   **要改必须重导 fixture** ⇒ 连带 `configHash` 与全部向量变化，属**不可逆数据操作** ⇒ 与 D 批（碰数据需先备份）同批，本轮不动。

5. **P3-22（`normalizeText` 对非 ASCII 系文字仍分叉）** —— **不改 + 理由**。
   覆盖表实测与清单逐条吻合（`isAlnumPythonish:45-62` 恰好只覆盖 ASCII 数字/字母、CJK 基本区、全角数字/字母、Latin-1、Latin Ext-A，**无西里尔/希腊/阿拉伯/CJK 扩展 B**）。但：
   - 分叉只在「同一输入在两引擎下取不同 gram」时发生，影响面**限 `normalizeText` 的消费者**（intent/echo 特征），**不影响记忆读写**；
   - 真对齐要引 Unicode 属性表，且需逐条对 Python `isalnum()` 的语义差 ⇒ 属**跨引擎契约级**改动。

   应并入「JS/Python 两引擎独立可互换」这条**既有铁律议题**统一裁定，不单独立项。清单自己也写了「影响面小，如实定级」——本轮同意该定级。

### 2.4 已覆盖（无需动作）—— 3 项

| ID | 已被谁覆盖 | 干系文件 |
|---|---|---|
| **P3-2** | 上游第五批 `task-7 sidecar-medic`（#107 看门狗 + #108 stderr 可见） | `lib/python-sidecar-client-pre.js`（352→629 行）、`tests/smoke/smoke-test-sidecar-watchdog-pre.mjs` |
| **P3-3(b)** | A 批同族修复（issue#58 的 activation-host per-runtime 回收接线） | `lib/activation-host-pre.js:564-570`、`lib/index.js:1233` |
| **P3-20①** | 上游第五批 `task-6 sec-webhook`（#113–#116 webhook fail-closed） | `.github/cloud/qq-webhook/index.js:556-568`、`.github/cloud/qq-webhook/index.zip` |

---

## 三、与已有批次的重叠（去重结果）

### 3.1 真重叠（该条目已被既有修复覆盖 ⇒ 从清单里划掉）—— 3 项

| #117 条目 | 覆盖它的批次 | 干系文件（实证） | 判定 |
|---|---|---|---|
| **P3-2** 侧车 respawn 的 `child=null` 竞态 / `rejectAll('crashed')` 不执行 / `stats.exits` 少计 | **上游第五批 `task-7`**（对应 issue #107 看门狗 + #108 stderr 可见） | `lib/python-sidecar-client-pre.js`（352→629 行，新增 `SIDECAR_WATCHDOG_PRE_V1`、`:610-615` 一次性结清 pending）；守卫 `tests/smoke/smoke-test-sidecar-watchdog-pre.mjs` | **√ 已覆盖**，无需重复修 |
| **P3-3(b)** `activation-host` 的 per-session 回收缺位 | **A 批同族（issue#58）** —— `dispose` 时补了 `_activationHost.disposeRuntime(runtime.key)` 接线 | `lib/activation-host-pre.js:564-570`（`runtimeState`/`stepsByRuntime` 双删）、`lib/index.js:1233` | **√ 已覆盖**（清单此处对 pre 线已过时） |
| **P3-20①** webhook 无请求体上限 | **上游第五批 `task-6`**（#116） | `.github/cloud/qq-webhook/index.js:556` `MAX_BODY_BYTES`（默认 1 MiB）+ `:561-568` 超限即拒；`.github/cloud/qq-webhook/index.zip` 已重建（mtime `2026-09-21T16:43`） | **√ 已覆盖** |

### 3.2 同源但**不同项**（不要误当重复而漏修）—— 4 组

去重时必须区分「同一条被修了」与「同一族里只修了一份」。以下 4 组是**后者**：

| #117 条目 | 与之同源但**已修**的对照物 | 结论 |
|---|---|---|
| **P3-3(a)** `context-host` 的 `pathsByKey` 无 per-session 出口 | 同族 `activation-host` 已修（`P3-3(b)`） | **未覆盖** ⇒ 两份实现只修了一份，P3-3(a) 仍是真缺口 |
| **P3-12** `config-io` 走裸 `renameSync` | 同族白板索引已修（上一轮 `#110` 修复，`lib/index.js:2533/2565` 用 `retryRename`） | **未覆盖** ⇒ `retryRename` 只接了 3 处，配置保存链漏接 |
| **P3-1(②)** `linkObserverCall` 重建 `callLinks` | 同族 `ingestEnvelope` 已有 `runtime.disposed` 闸（`:1535`） | **未覆盖** ⇒ 同一防线漏一个入口 |
| **P3-20②③** 设置端点无类型/路径校验、下载流无字节上限 | 同族 webhook 已修（`P3-20①`） | **未覆盖** ⇒ 三处边界只修了一处 |

### 3.3 与 A/B/C/D/E 批的关系

- **A 批 9 条**（`docs/internal/BUGLIST-persistent-memory-20260921.md` §二）：**与 #117 无交集**。A 批集中在 `fact-store-pre`/`episodic-store-pre`/`procedure-store-pre`/`note-status-pre`/`intent-clean-safe-pre` 的**记忆语义**层；#117 集中在 `index.js` 的**生命周期/诊断/结构**层 ⇒ 无重叠条目。
- **B 批 4 条**：B-2（`procedurePromotionEnabled` 语义错配）已按用户拍板拆成契约模块，落点 `lib/procedure-switch-pre.js` 并已改 `lib/context-host-pre.js` / `lib/activation-host-pre.js` 的消费点 ⇒ **与 #117 无交集**（#117 里没有 procedure 语义项）。
- **C 批 C-3**（26 对 `-pre`/非 `-pre` 漂移 + 删/隔离）：**这是 #117 P3-13 与 P3-14 的共同上游**。本报告实测确认 26 对同名文件确实存在且都漂移（例如 `context-host-pre.js` 53642 B / `context-host.js` 42281 B，mtime 差 14 天）。
  ⇒ **建议**：P3-13/P3-14 不要独立立项，**并入 C-3 一次裁定**（C-3 已写明「删前必做：核对 `tests/` + `tools/` 对非 `-pre` 文件的引用」——这一步 #117 也没做）。
- **D 批 D-2**（`~/.dsh/memory/hub/` 与 `hub-pre/` 并存 ⇒ 改名隔离）：**与 #117 P3-13 同族**（都是「`-pre` 落到了磁盘数据路径」）⇒ 同样并入 C-3/D-2 的显式决策。
- **E 批 E-7**（静默吞错清单，建议统一走 diag + degradeSink）：**与 #117 P3-10/P3-11 高度重叠**（都是「catch 不留痕」）。E-7 已点名 `index.js` 的 `:6370/:6378/:6381/:6458/:5445/:10063/:10071/:10085/:9476/:4707` 等；P3-10 点名的是**路由层** catch。
  ⇒ **建议合并为一次「留痕统一改造」**，避免两次动 `index.js` 的 catch 面。
- **上游 #103–#116（本轮第四/五批）**：逐条比对后，除 3.1 的 3 项真重叠外无其它交集（#106 打包负向排除 / #109 策略一致性锁 / #110 写失败可见 / #111–#112 文档与套件隔离 / #113–#116 webhook 均不与 #117 的条目重合）。

### 3.4 去重后的净待办数

| 分类 | 条数 | 说明 |
|---|---|---|
| 原始清单 | 22 | |
| 减去 已覆盖 | −3 | P3-2、P3-3(b)、P3-20① |
| 减去 误报/子项误判 | −2（+1 子项） | P3-15（整项误报）、P3-3(d)（对照物描述不准）、P3-18 的一条因果（「因此常红」不成立）、P3-20① 同属已覆盖 |
| **净待办** | **19 项 + 2 子项** | 其中「立即修 7」「可延后 11（含 2 子项）」「判定不改 5」 |

---

## 四、不确定清单（无法判定为真或假）

> 以下条目**本轮没能给出真/假判定**，逐条写明缺什么信息。**不得把它们当结论使用。**

| ID | 卡在哪 | 缺什么信息 / 动作 |
|---|---|---|
| **P3-5** | **无法判定「当下是否可触发」**。代码事实（跨 await RMW + `memoryAnchorEnabled` 默认 false）已确认成立，但「是否存在两个写者并发同一 `.md`」无法从静态阅读判定：调用方（maintain / hubFlushTick / note-status）各自的串行门是否**覆盖全部路径**需要逐个追。 | 需要一次**并发探针**：构造两个 agent 同 workspace 同时调 `memory_note_pre`（或直接对同一 `p` 并发 `appendText`），观察是否出现「后者覆盖前者」。清单自述也是「待观察」，本轮未升级结论。 |
| **P3-16** | **无法逐条判定**：清单只说「全仓 19 个零引用 export」并给 2 个例子，**没给名单**。本轮抽样验证的 2 例（`tier-layer-inject-pre.js:115`、`m7-wire-pre.js:207`）成立，但「19」这个数字与其余 17 个无法核。 | 需要**完整名单**（或授权做一次全量 export 引用扫描）。做扫描本身要构造「全仓 ESM 导出 → 引用点」的静态分析，超本轮只读取证的范围。 |
| **P3-20③** | **未核**：模型下载流的字节上限。清单给的位置是 `lib/semantic-js.js:445-478`，pre 线对位在 `lib/semantic-js-pre.js`（552 行），本轮**未读该段落**。 | 需要读 `lib/semantic-js-pre.js` 的下载流段落，确认 `total` 是否只用于进度显示、是否无 byte cap。属一次阅读即可判定，成本低。 |
| **P3-1（①）的后果** | **「存在性」已判定为真，「后果」无法判定**。`abortController.signal` 无消费者是硬事实；但「dispose 后在途 await 仍可继续」会**造成什么用户可见后果**（残留写入？幻影 runtime？）本轮未构造。清单自己也标注「后果为静态推演」。 | 需要一次**dispose 竞态探针**：在 `agent/disposed` 后让一个在途 `await` 完成，观察是否落盘/是否复活 runtime。若造不出来，按「不改」处置即可（本报告已这么定）。 |
| **P3-12 的同类面** | **「是否还有别的漏接处」无法判定**：本轮确认 `retryRename` 全仓接线 3 处（`index.js:2533/2565`、`memory-writer-pre.js:258`、`subagent-gc-pre.js:264`）、裸 `renameSync` 另有 7 处（`config-io-pre.js:61/77/97`、`index.js:9308`、`python-setup-pre.js:300`、`semantic-js-pre.js:514`、`skill-export-host-pre.js:119`）。剩下 6 处**哪些该接**需要按「目标文件是否可能被 Windows 句柄占用」逐个判断，本轮未做。 | 需要一次**按目标文件分类**的判断（模型文件/配置/技能导出/派生状态各自的风险等级）。 |
| **P3-11 的「该不该留痕」** | **不是判不出真假，而是判不出取舍**：`dispose()` 返回 `false` 的三种原因（无 agent / 无 runtime / 是 default）**有正常语义**，不能一律留痕否则又会刷屏。哪些分支该留痕是设计决策。 | 需要一次**口径裁定**（与 P3-9 的节流策略一起定）：只对「该有却来找不到」这一类留痕。 |
| **P3-19 的「是否值得拆」** | **数据已实测**（1071 行 / 49 handler / 248L / 319L / 229L），但「拆到什么粒度才不被断言绑死」需要先摸清断言面。 | 需要一次**断言依赖扫描**：`tests/smoke/*` 里有多少条断言依赖这几个函数的当前源码形状（本轮已知 `smoke-test-batch11-20260919-pre.mjs:174-180` 就在用「限定作用域内断言」的写法）。 |

---

## 五、如果只做 3 条，做哪 3 条（按「用户能感知的影响 ÷ 改动风险」排序）

### 第 1 名 · P3-6（`polling-heartbeat.json` 的 `queueLength` 恒 undefined）

- **用户能感知的影响**：高。这是**唯一一个「用户/运维者按手册去看一眼就能判断系统死活」的探针**（心跳时间戳 + 队列长度），而 `queueLength` 从写下那一刻起就是 `undefined` ⇒ `JSON.stringify` 直接丢键 ⇒ 用户按字段找永远找不到，会误判成「队列功能没做」。
- **改动风险**：极低。`lib/index.js:11628` `queueLength: q.length` → `queueLength: q`，`q` 在 `:11621` 就是 `reduce(...)` 的数字结果，类型正确、无副作用、无 await、无并发。
- **额外价值**：修的同时暴露了**文档缺口** —— 实测 `polling-heartbeat` 在 `docs/**` + 两份 README 里**命中 0 次**，所以这条的正确修法是「改 1 行代码 + 补 1 句文档」，一次把「探针存在但没人知道」也解决掉。
- **为什么排第 1**：影响/风险的比值最高 —— 一行代码换一个「用户真的会去看的观测面」，且零回归面。

### 第 2 名 · P3-14（开发树 `../artifacts/m7-live-pre/*` 路径进发布包）

- **用户能感知的影响**：中。用户不会直接看到这个路径，但它**确实会进发布包**（`tools/release.mjs:234` 的转换表把 `semantic-js-pre.js` 列为发布转换对象 ⇒ `-pre` 内容就是发布内容），且它是 peer/模型探测的**候选路径之一**，在用户机上恒为不存在路径。属「产品里带着维护者机器的痕迹」这类会被用户翻出来的问题（对照：本仓 `package.json.files` 已因 #106 移除 `docs`，说明这类「产物不该带什么」的问题用户在意）。
- **改动风险**：极低。三个候选路径（`:79`/`:95`/`:183`）加一个 `process.env` 闸或直接删 —— **删掉后用户侧行为零变化**（该路径在用户机上本就不存在，删的是死候选），维护者侧用 env 闸保留能力。
- **为什么排第 2**：与第 1 名同为「零风险」，但影响是「产物品相 + 探测面干净」而非直接观测面，故次之。

### 第 3 名 · P3-9（recall 主路径 3 处 + shadow/evidence 2 处裸 diag，只改 6 点）

- **用户能感知的影响**：高，且**有事故前科**。`lib/index.js:856-872` 的注释就是自述事故：「每 tick 都失败且都打日志 ⇒ 在宿主控制台无限刷屏，用户侧表现为『打开就一直弹这些消息』」。而 recall 主路径三处（`:6259` 语义臂降级 / `:6298` evidence-agg 降级 / `:6311` temporal-parse 降级）+ `shadow-host-pre.js:228`（`no-paths`）+ `evidence-store-pre.js:143`（`write-failed`）**都是持续性故障会每轮复现的面**。用户对「控制台被刷」的敏感度极高（本仓已有一次实际投诉）。
- **改动风险**：低但**不为零** —— 需要克制：只把这 6 个调用点换成 `diagThrottled(key, msg)`（`lib/index.js:864` 已有现成函数，5 分钟窗口），**不做全仓替换**。风险点在于「这 5 个边有两个文件不在 `index.js` 内」，需要把 `diagThrottled` 的等价值带过去（或经由现有 `onDiag` 回调转一手）⇒ 比前两名多一个「跨文件一致性」的小决策。
- **为什么排第 3**：影响面与第 1 名同级（甚至更大），但风险略高（跨文件）、且它属于「防复发」而非「修现状」，故排第三。

### 为什么这 3 条而不是别的「立即修」条目

| 被挤下去的条目 | 挤下去的原因 |
|---|---|
| P3-3(a) | 真缺陷且一行修，但**用户不可感知**（长驻进程内存单调增长，用户看不到）；属「该修但不急」。**它是第 4 名**，若下一轮能带 4 条应带上它。 |
| P3-1(②) | 同上：真缺陷、3 行修，但**缺一个已证实的触发场景**（`linkObserverCall` 的调用者能否拿到已 dispose 的 runtime 未构造），风险/收益比不如前 3。 |
| P3-12 | 影响真实（Windows 上设置页保存失败），但**改了也只覆盖一条链**，剩余 6 处裸 `renameSync` 未定 ⇒ 半修反而制造「已修」错觉 ⇒ 更适合与「重命名原子性统一」一起做。 |
| P3-20② | 影响是「护栏缺失」而非现时症状，且限于 loopback + GUI 同信任域 ⇒ 与 P3-12 同属「安全/护栏批次」，应整批做。 |

---

## 六、取证命令（便于 Lead 复核）

全部为**只读**操作，未改任何 `lib/**` / `tests/**` / `tools/**` 文件，未提交、未 push、未重启任何进程。

```powershell
# 1) 宿主 import 全部指向 -pre.js（判定「改 plain 文件无效」的根据）
Select-String -Path lib/index.js -Pattern "(?:require\(|from\s+)['""]\.\/" | ForEach-Object { $_.LineNumber.ToString().PadLeft(6) + "  " + $_.Line.Trim() }

# 2) pre / plain 同名文件的存在性 + mtime + size（证明 plain 是 09-07 发布产物）
node -e "..."   # 见 .vision-tmp/_probe117-*.cjs 系列；核心是对 39 条 import 逐个 statSync 并 statSync 去掉 -pre 的同名文件

# 3) abortController 全仓消费者（P3-1 ①的证据：5 处全是构造+abort，零 .signal 读点）
node .vision-tmp/_probe117-abort.cjs

# 4) B 段诊断锚点全仓扫描（P3-6/7/8/9 的名词一次扫清）
node .vision-tmp/_probe117-b.cjs

# 5) P3-12 的三套原子写 + retryRename 接线面
node .vision-tmp/_probe117-l.cjs

# 6) P3-13/14/15/16 的精确代码位置（排除 md 与 .vision-tmp）
node .vision-tmp/_probe117-i.cjs

# 7) P3-17/18 的版本与谱系
node .vision-tmp/_probe117-t.cjs ; node .vision-tmp/_probe117-u.cjs

# 8) P3-19 的结构计数（routes 数组边界 / defineTool 数 / 函数行数）
node .vision-tmp/_probe117-o.cjs ; node .vision-tmp/_probe117-p.cjs ; node .vision-tmp/_probe117-q.cjs

# 9) P3-21 死分支（policy thresholds + 双侧条件）
node .vision-tmp/_probe117-r.cjs

# 10) 确认本报告未触碰其他文件
git status --porcelain=v1
```

**关键交叉核对（防单次 grep 误判）**：
- `abortController` 一处 grep ≠ 结论 ⇒ 另跑了全仓 329 个 `js/mjs/cjs` 的穷举扫描（`_probe117-abort.cjs`），两次结论一致。
- 「`pathsByKey` 无出口」⇒ 用 `delete|clear` 与 `disposeSession` 两组模式各扫一次，两组都为空（`_probe117-g.cjs`）。
- 「`:978` 注释失真」⇒ 用 `defineTool(` 计数（17）+ routes-array `handler:` 计数（49）+ `path:` 计数（80）三个独立口径互证。
- 「P3-15 误报」⇒ 除 `existsSync` 外，另跑全仓（含 `tests/`/`tools/`）对 `acceptance.js` 的字符串扫描，命中 0。

---

## 七、给上游回帖的建议口径（供 Lead 直接用）

只回**已经能定论**的部分，不要一次把 22 项都退回：

1. **已覆盖 3 项**（P3-2 / P3-3(b) / P3-20①）：写明对应修复批次与守卫名，请上游据此关闭这几项。
2. **误报 1 项**（P3-15）：给出反证（`lib/acceptance.js` 在 pre 线不存在、pre 线对位文件 `acceptance-pre.js` 是被测对象而非发布门），请上游确认是否为 `main` 线残留描述。
3. **立即修 7 项**（P3-1②/P3-3(a)/P3-6/P3-9/P3-12/P3-14/P3-20②）：写明「已列入下一轮、只做最小面」，并**逐条注明限制**（如 P3-12 只改 async 版、P3-9 只改 6 个调用点），避免被读成「整项承诺修完」。
4. **判定不改 5 项**（P3-1①/P3-5/P3-15/P3-18/P3-22）：**逐条给理由**（本报告 §2.3 可直接摘用）。特别是 P3-1① 与 P3-22 要写清「已核为真、按设计/按既有议题处理」，否则容易被读成漏项。
5. **须上游给输入才能动的 3 项**：P3-13（需改名决策，牵动用户磁盘目录）、P3-16（需 19 个 export 的完整名单）、P3-20③（需先核）。这三项应在回帖里**明确提出问题**而不是留白。
6. **口径纠错 3 条**（体现核验深度、也帮上游校正其审计脚本）：
   - 清单以 plain 文件（`lib/*.js`）行号定位，而宿主只 import `-pre.js` ⇒ **行号与实际运行代码不对应**（P3-2/P3-3/P3-8/P3-12/P3-13/P3-14/P3-15/P3-19 均受影响）；
   - P3-18「`smoke-test-m72-pre.mjs:28` 因此常红」不成立（该断言与 fixture 现值一致）；
   - P3-6 所述「手册让运维者确认轮询活着」的探针**在文档里不存在**（`docs/**` + 两份 README 对 `polling-heartbeat` 命中 0）⇒ 该条比清单描述的更需要修，但理由与清单不同。

---

> 本报告仅写入 `docs/internal/ISSUE117-TRIAGE-20260922.md` 一个文件；`lib/**`、`tests/**`、`tools/**`、`docs/` 下其它文件均未修改；未做任何 git 提交或推送；未重启或终止任何进程。
> 取证脚本落在 `.vision-tmp/_probe117-*.cjs`（只读探针，不在交付范围内，可按需删除）。
