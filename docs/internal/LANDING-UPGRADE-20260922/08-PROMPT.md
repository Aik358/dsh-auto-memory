# 08 · PROMPT — 交给 K3 的唯一指令

> **用法**：压缩上下文、切到 K3 之后，**把下面 `═══` 之间的内容整段复制粘贴给 K3**。不要附加其它说明，不要贴本文件以外的上下文。K3 会自己读盘。

---

═══ 复制从这里开始 ═══

你是资深设计工程师 + 前端实现者。任务：**升级一个单文件落地页，然后推到 GitHub `main` 分支**。

## 第一步（必做）：读施工单

按顺序读这些文件（都在 `D:\dsh-auto-memory\docs\internal\LANDING-UPGRADE-20260922\`）：

1. `README.md` —— 总纲、执行纪律、**本页三条专属禁令**、已拍板结论
2. `00-AUDIT.md` —— 现状实测数据 + **10 条否决清单**（否决项不要实现）
3. `01-FOUNDATION.md` → `02-SCROLL.md` → `03-COMPONENTS.md` → `04-MOBILE.md` → `05-CONTENT.md` —— 五个施工计划，**按编号顺序执行**
4. `06-VERIFY.md` —— 验收脚本（可直接跑）
5. `07-PUSH-TO-MAIN.md` —— 推送流程（**有硬约束，务必读完再动 git**）

**读完先不要动代码。** 先向我复述：改哪里、改几处、按什么顺序、有哪三条红线。确认后再开工。

## 目标文件

`D:\dsh-auto-memory\docs\landing\index.html`
（单文件、零外部依赖、内联 CSS + JS；1,745 行 / 125,060 字节 / CRLF）

## 五条红线（违反即任务失败）

1. **绝不切分支、绝不 `git checkout`、绝不改 `lib/` 下任何文件。**
   `D:\dsh-auto-memory` 是 `link:` 挂载的**活宿主代码树**，DSH 宿主正在运行它。切分支会让宿主读到另一份代码并可能崩溃。
   ⇒ 所有 git 操作只在独立 worktree 里做（`07-PUSH-TO-MAIN.md` §2 给了完整命令）。

2. **绝不 `git push --force`。**
   本地 `main` 与 `origin/main` 已分叉（实测 72 ↔ 144）。施工单给的 worktree 方案是**快进推送**，不需要 force。force 会抹掉远端 72 个提交（含 release 线）。

3. **不动这三处**（它们是**历史事实**，改了就是篡改）：
   - `v0.1.30` 的**历史引用**：「3.3 · 欢迎向导（v0.1.30 大更新）」「v0.1.30 起所有用户升级后自动播放一次」
   - 「十二个能力」（实测 `.feature` 恰 12 个，**正确**）
   - 「七张实机截图」（实测 `.g-card` 恰 7 个，**正确**）
   验收断言：改完后全页 `v0.1.30` 应**恰好剩 3 处**（不是 0）。

4. **不删除既有动效。** 页面里 3 个 `@keyframes`（`blink` / `fill` / `cell-hit`）是「系统示意图」的语义表达（数据在管线中流动），**不在改动范围内**。

5. **不引入任何外部依赖。** 不加 CDN、不加字体、不加动效库、不加构建步骤。这页必须保持单文件自包含。

## 关键实现纪律

- **照抄施工单给的代码块**。所有时长/缓动/位移/缩放/模糊都用 `var(--duration-*)` / `var(--ease-*)` token，**不要自己写数值**。
- **JS 必须 ES5 风格**：`var` + `function(){}`。这页不用箭头函数、不用模板串、不用 `const`。范本见文件内 `L1595–1690`。
- **双语同步**：任何新增文案都要同时进 `zh`（`L1316` 起）与 `en`（`L1451` 起）两份字典。
- **执行顺序固定**：01 → 02 → 03 → 04 → 05。01 是底座（token + 双闸门 + 按压反馈），后面四个都引用它。
- **验收必须走渲染取证**：这页是单文件内联，`grep` 只能证明「字符串写进去了」，**不能证明 CSS 生效**。
  用 `06-VERIFY.md` §1 给的 headless Chrome 探针（复制页面 → 注入探针 → `--dump-dom` 读 `getComputedStyle` → 双视口截图）。**不要用静态断言代替渲染验证。**

## 一个已知的真实缺陷（顺手修，属于 05 计划）

轮播 `.promo-track img` 用了 `aspect-ratio:16/9` + `object-fit:cover`，但 3:2 的图（`1536×1024`）会被**上下各裁掉约 11%**，切到画面标题。CSS 里已有 `background:var(--paper-2)` —— 纸色底只有在 `contain` 下才有意义，说明**原意就是 `contain`**。改成 `contain`。

## 完成后必须做的三件事

1. **跑 `06-VERIFY.md` 全部机械断言**，把探针 JSON 原样贴给我。
2. **按 `07-PUSH-TO-MAIN.md` 推送**到 `origin/main`（worktree 方案，**不切分支、不 force**），然后做该文件 §4 的三处验证 + 用新 URL 实测线上页面：
   `https://htmlpreview.github.io/?https://github.com/Aik358/dsh-auto-memory/blob/main/docs/landing/index.html`
   （注意：**URL 从 `blob/preview/` 变成了 `blob/main/`**）
3. **明确交底**：哪几条是真机才能验的（点按延迟 / hover 粘滞 / 橡皮筋 / 安全区 —— 设备模拟**测不出来**），**不要**用模拟器截图宣称移动端已修好。

## 报告格式

- 改了哪些行、共几处（按 5 个计划分组）
- 探针 JSON 全量输出
- 两张截图路径（桌面 + 390px）
- 线上 URL 实测结果
- **真机待确认清单**（明确标注「未验证」）

不确定的地方**停下来问**，不要即兴发挥。施工单里凡是标了「若与代码不符，停下报告」的地方，照做。

═══ 复制到这里结束 ═══
